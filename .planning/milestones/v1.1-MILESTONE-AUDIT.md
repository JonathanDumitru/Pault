---
milestone: v1.1
audited: 2026-04-29T18:00:00Z
status: passed
scores:
  requirements: 12/12
  phases: 5/5
  integration: 6/6
  flows: 6/6
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: "14-data-integrity-code-quality"
    items:
      - "Legacy single-arg CopyEvent init still exists in CopyEvent.swift:32 but has zero production callers — could be removed in a future cleanup."
  - phase: "16-testing-verification"
    items:
      - "ScreenshotTests.swift contains 5 fixed-duration Thread.sleep calls between navigation steps (not as substitutes for waitForExistence). Standard UI-test pacing but worth replacing with explicit waits in a future iteration."
      - "menu-bar-content accessibility identifier exists in production (MenuBarContentView:147) but is not queried by any test by design — testShot05 navigates via app.menuBars.buttons['Pault']. Identifier is dead-code-adjacent unless future tests use it."
  - phase: "17-screenshot-test-reachability"
    items:
      - "Operational constraint (informational, not a code gap): macOS Stage Manager interferes with XCUITest window discovery. Future CI configuration should explicitly disable Stage Manager before invoking screenshot test suites. Documented in 17-01-SUMMARY.md."
nyquist:
  compliant_phases: ["13"]
  partial_phases: ["15"]
  missing_phases: ["14", "16", "17"]
  overall: "partial"
  note: "Out-of-scope per REQUIREMENTS.md:48 — Nyquist compliance retrofit explicitly deferred for v1.1. Discovery only."
---

# Milestone v1.1 Tech Debt Cleanup — Audit Report

**Audited:** 2026-04-29 (refresh — supersedes initial 2026-04-29 audit after Phase 17 closure)
**Phases:** 13, 14, 15, 16, 17 (all completed)
**Status:** ✓ passed

## Executive Summary

All five v1.1 phases passed individual verification and all 12 milestone requirements are fully satisfied across the three independent sources (VERIFICATION.md, SUMMARY frontmatter, REQUIREMENTS.md traceability). The two partial requirements identified in the initial audit (TEST-01, UX-03) were closed by Phase 17 — the Analytics toolbar Button now exposes `.accessibilityLabel("Analytics")`, the `analytics-view` identifier was relocated into the rendered NavigationStack subtree where XCUI can resolve it, and `ProStatusManager.init()` applies the `--screenshot-mode` override synchronously before scheduling the async refresh Task. End-to-end test `testShot06_AnalyticsDashboard` runs to completion and produces screenshot 06.

Cross-phase integration check (`.planning/phases/v1.1-INTEGRATION-CHECK.md`) verified all 6 of 6 milestone flows end-to-end. No broken flows, no missing wiring, no orphaned exports. Tech debt is minor and tracked.

## Requirements Coverage (3-Source Cross-Reference)

| REQ-ID | VERIFICATION.md | SUMMARY frontmatter | REQUIREMENTS.md | Final Status |
|--------|-----------------|---------------------|-----------------|--------------|
| DOC-01 | passed (Phase 13) | listed (13-01) | `[x]` Phase 13 | **satisfied** |
| DOC-02 | passed (Phase 13) | listed (13-01) | `[x]` Phase 13 | **satisfied** |
| DOC-03 | passed (Phase 13) | listed (13-01) | `[x]` Phase 13 | **satisfied** |
| DATA-01 | passed (Phase 14) | listed (14-01) | `[x]` Phase 14 | **satisfied** |
| DATA-02 | passed (Phase 14) | listed (14-01) | `[x]` Phase 14 | **satisfied** |
| CODE-01 | passed (Phase 14) | listed (14-01) | `[x]` Phase 14 | **satisfied** |
| UX-01 | passed (Phase 15) | listed (15-01) | `[x]` Phase 15 | **satisfied** |
| UX-02 | passed (Phase 15) | listed (15-01) | `[x]` Phase 15 | **satisfied** |
| UX-03 | passed (Phase 15 + Phase 17) | listed (15-01, 17-01) | `[x]` Phase 17 | **satisfied** (Phase 17 closure) |
| TEST-01 | passed (Phase 16 + Phase 17) | listed (16-01, 17-01) | `[x]` Phase 17 | **satisfied** (Phase 17 closure) |
| TEST-02 | passed (Phase 16) | listed (16-02) | `[x]` Phase 16 | **satisfied** |
| TEST-03 | passed (Phase 16) | listed (16-02) | `[x]` Phase 16 | **satisfied** |

**Score:** 12/12 fully satisfied · 0 partial · 0 unsatisfied · 0 orphaned

No orphaned requirements detected — every REQ-ID in REQUIREMENTS.md is claimed by at least one phase SUMMARY and has a corresponding VERIFICATION.md entry.

## Phase-Level Verification Results

| Phase | Status | Score | Notes |
|-------|--------|-------|-------|
| 13 — Documentation & Legal | passed | 3/3 truths | All 33 v1.0 traceability rows Complete; Phase 04 SUMMARYs populated; legal date replaced. No anti-patterns. |
| 14 — Data Integrity & Code Quality | passed | 3/3 truths | Attachment round-trip restored in both insert/update paths; CopyEvent two-arg init at all 3 call sites; SidebarView delegates to PromptService.filterPrompts at all 4 sites. 4 commits verified in git. |
| 15 — UX Polish | passed | 4/4 truths | noProxyStateView guard, refresh button on .aiCurated collections, --screenshot-mode override (later hardened by Phase 17). 4 human-verification items completed during plan execution. |
| 16 — Testing & Verification | passed | 5/5 truths | 5 production views got accessibility identifiers; ScreenshotTests updated; 10 human-verification items signed off in 16-02-SUMMARY.md. |
| 17 — Screenshot Test Reachability | passed | 3/3 truths | Analytics toolbar `.accessibilityLabel("Analytics")` added (ContentView:354); AnalyticsView identifier moved into rendered Group inside NavigationStack (Rule-3 deviation, dedicated commit 94d6966); synchronous `--screenshot-mode` override in ProStatusManager.init() before async Task. testShot06 ran to completion, screenshot 06 captured. |

## Integration Findings

Cross-phase integration check verified 6 of 6 end-to-end flows. Full report: `.planning/phases/v1.1-INTEGRATION-CHECK.md`.

### Verified Flows

1. **Export → Import round-trip with attachments** (DATA-01) — Both `ImportOrchestrator.insertPrompt:300-314` and `updatePrompt:391-409` restore Attachment stubs from `record.attachmentFileNames`.
2. **Smart-collection filtering through SidebarView** (CODE-01) — `filteredPrompts:82-84`, `collectionCounts:117`, and both context menu export paths (lines 226, 234) delegate to `PromptService.filterPrompts`. Zero inline filter logic remains.
3. **First-run AI flow with no proxy** (UX-01 + UX-02) — `AIAssistPanel.body` checks `!isProxyConfigured` before `!hasAnyAPIKey` (lines 64-67). Sidebar refresh button `.disabled(!ProxyConfig.isConfigured)` (SidebarView:215). SmartCollectionEditorView Generate-with-AI button likewise gated (line 96).
4. **Clipboard copy** (DATA-02) — All three production CopyEvent call sites use the two-arg form (PromptService:95, AnalyticsService:26, ScreenshotDataSeeder:437). Legacy single-arg init exists in CopyEvent.swift:32 but has zero callers.
5. **Documentation traceability vs reality** (DOC-01 + DOC-02 + DOC-03) — Phase 04 SUMMARYs collectively cover {R2.1–R2.5, R5.1–R5.3}, exactly the 8 requirements the traceability table attributes to Phase 4. Legal docs have `2026-04-27` in place of `[Launch Date]` (line 3 in each).
6. **Screenshot mode showing Pro UI — testShot06_AnalyticsDashboard** (UX-03 + TEST-01) — Closed by Phase 17. Synchronous Pro override in `ProStatusManager.init():28-32` flips `isProUnlocked = true` before any view body renders. ContentView toolbar Button `.accessibilityLabel("Analytics")` at line 354 resolves `app.buttons["Analytics"]` query at ScreenshotTests.swift:304. AnalyticsView identifier at line 64 is inside the rendered Group within NavigationStack so `app.groups["analytics-view"]` resolves once the sheet presents. Test passed end-to-end with screenshot captured.

### Broken Flows

None. The single broken flow in the initial audit was Phase 17's reason to exist; it is now wired.

## Phase 17 — Gap Closure Detail

The initial v1.1 audit (2026-04-29 morning) flagged TEST-01 and UX-03 as `partial` due to two integration gaps and one broken end-to-end flow. Phase 17 was scoped specifically to close those gaps without introducing new functionality:

| Original Gap | Closure Mechanism | File:Line | Verification |
|--------------|-------------------|-----------|--------------|
| Toolbar button has `.help()` tooltip but no AX label — XCUI cannot match `app.buttons["Analytics"]` | Added `.accessibilityLabel("Analytics")` between `.buttonStyle(.plain)` and `.help("Analytics")` | ContentView.swift:354 | testShot06 click branch confirmed (no fallback path triggered) |
| ProStatusManager scheduled `--screenshot-mode` override async — first frame shows Pro UI hidden | Added synchronous `#if DEBUG` block setting `isProUnlocked = true` before `Task { await refreshStatus() }` | ProStatusManager.swift:28-32 | First-frame race eliminated; refreshStatus retains the same guard at lines 70-74 for the async path |
| `analytics-view` identifier on `NavigationStack` wrapper did not propagate to rendered subtree (Rule-3 deviation surfaced during execution) | Relocated `.accessibilityIdentifier("analytics-view")` into the rendered Group inside NavigationStack content closure | AnalyticsView.swift:64 | `app.groups["analytics-view"]` resolves at runtime; testShot06 sheet detection passes |

Phase 17 verification confirmed all three closures with file-line citations and test execution evidence (xcodebuild test exit 0, screenshot file at `/tmp/06-analytics-dashboard-final.png`, user visual sign-off).

## Tech Debt Inventory

### Phase 14
- Legacy single-arg `CopyEvent` init still exists in `CopyEvent.swift:32` but has zero production callers. Could be removed in a future cleanup.

### Phase 16
- `ScreenshotTests.swift` contains 5 fixed-duration `Thread.sleep` calls between navigation steps. Standard UI-test pacing but worth replacing with explicit `waitForExistence` waits in a future iteration.
- `menu-bar-content` accessibility identifier exists in production (`MenuBarContentView:147`) but is not queried by any test — `testShot05` navigates via `app.menuBars.buttons["Pault"]` instead. Identifier is dead-code-adjacent unless future tests use it.

### Phase 17
- Operational constraint (informational, not a code gap): macOS Stage Manager interferes with XCUITest window discovery. Future CI configuration should explicitly disable Stage Manager before invoking screenshot test suites. Documented in 17-01-SUMMARY.md.

## Nyquist Compliance

| Phase | VALIDATION.md | nyquist_compliant | wave_0_complete | Status |
|-------|---------------|-------------------|-----------------|--------|
| 13 | exists | true | true | COMPLIANT |
| 14 | **missing** | — | — | MISSING |
| 15 | exists | false | false | PARTIAL |
| 16 | **missing** | — | — | MISSING |
| 17 | **missing** | — | — | MISSING |

**Overall:** PARTIAL. Four of five phases lack full Nyquist coverage. Per workflow, this is discovery-only — the milestone audit does not auto-trigger validation.

This is consistent with the v1.1 milestone scope, which explicitly listed "Nyquist compliance retrofit" as out-of-scope (REQUIREMENTS.md:48). The gap is acknowledged, not unintentional. To address in a separate effort: run `/gsd:validate-phase 14`, `/gsd:validate-phase 15`, `/gsd:validate-phase 16`, `/gsd:validate-phase 17`.

## Deviations from Plan

One documented Rule-3 deviation in Phase 17: `Pault/AnalyticsView.swift` was modified beyond the original `files_modified` scope (originally only ContentView.swift and ProStatusManager.swift) to relocate the `analytics-view` AX identifier into the rendered subtree. The deviation was necessary for end-to-end test success and is documented in 17-01-SUMMARY.md with a dedicated commit (`94d6966`).

No other deviations. Each phase shipped exactly the plans declared in the roadmap (1/1, 1/1, 1/1, 2/2, 1/1 plans). No scope creep, no missing commits, no anti-patterns.

---

_Audit performed by Claude (gsd:audit-milestone)_
_Source data: 5 phase VERIFICATION.md files, 6 plan SUMMARY.md frontmatter blocks, REQUIREMENTS.md traceability table, integration checker subagent report (.planning/phases/v1.1-INTEGRATION-CHECK.md)._
_Supersedes initial 2026-04-29 audit which flagged TEST-01 and UX-03 as partial; both closed by Phase 17._
