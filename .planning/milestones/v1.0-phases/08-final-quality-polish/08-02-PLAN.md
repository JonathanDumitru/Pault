---
phase: 08-final-quality-polish
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - PaultTests/PerformanceBenchmarkTests.swift
  - PaultUITests/PaultUITests.swift
  - Pault/**/*.swift
autonomous: false
requirements: [R8.3, R1.4]

must_haves:
  truths:
    - "App launches in under 1 second in Release configuration"
    - "Canvas compilation with 20+ blocks completes in under 300ms"
    - "Slash command palette filter completes in under 10ms"
    - "No memory leaks detected in a 5-minute editing session"
    - "Memory footprint remains stable during extended editing (no unbounded growth)"
  artifacts:
    - path: "PaultTests/PerformanceBenchmarkTests.swift"
      provides: "Performance benchmarks passing in Release config"
    - path: "PaultUITests/PaultUITests.swift"
      provides: "Launch performance measurement with baseline"
  key_links:
    - from: "PaultTests/PerformanceBenchmarkTests.swift"
      to: "Pault/BlockEditor/PromptStudioModel.swift"
      via: "measure() blocks calling compileNow(), filterBlocks()"
      pattern: "measure.*compileNow|filterBlocks"
    - from: "PaultUITests/PaultUITests.swift"
      to: "XCTApplicationLaunchMetric"
      via: "testLaunchPerformance() measure block"
      pattern: "XCTApplicationLaunchMetric"
---

<objective>
Profile the app's performance against hard targets (launch < 1s, canvas sync < 300ms, palette filter < 10ms), fix any bottlenecks found, and verify no memory leaks exist.

Purpose: Ensure the shipped app meets performance targets. Must run after bug scrub (08-01) because bug fixes can change performance characteristics.
Output: All performance benchmarks passing, any SwiftData or view body optimizations applied.
</objective>

<execution_context>
@/Users/dev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-final-quality-polish/08-RESEARCH.md
@.planning/phases/08-final-quality-polish/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run performance benchmarks in Release config and fix bottlenecks</name>
  <files>PaultTests/PerformanceBenchmarkTests.swift, Pault/**/*.swift</files>
  <action>
    1. Run existing performance benchmarks in Release configuration:
       `xcodebuild test -scheme Pault -destination 'platform=macOS' -configuration Release -only-testing PaultTests/PerformanceBenchmarkTests 2>&1 | xcpretty`
    2. Verify all 3 existing benchmarks pass their baselines:
       - compileNow() with 20+ blocks: < 300ms
       - SlashCommandState.filterBlocks(): < 10ms
       - Sequential 20-block add: no O(n^2) degradation
    3. If any benchmark fails its target, profile the specific operation:
       - For compileNow bottleneck: Check if CompilationCache is being invalidated unnecessarily. Look for SwiftData fetch in the compilation path. Apply `fetchLimit` and `relationshipKeyPathsForPrefetching` if SwiftData queries are in the hot path.
       - For filterBlocks bottleneck: Check if block registry loading is synchronous on every filter call. Cache the registry.
       - For sequential add: Check if compileNow is called on every add (should be debounced).
    4. Run launch performance test in Release config:
       `xcodebuild test -scheme Pault -destination 'platform=macOS' -configuration Release -only-testing PaultUITests/PaultUITests/testLaunchPerformance 2>&1 | xcpretty`
    5. If launch exceeds 1s, investigate:
       - Check for synchronous SwiftData migrations on launch
       - Check for expensive static initializers
       - Check GlobalHotkeyManager Carbon event registration timing
    6. Apply any SwiftData query optimizations found (add predicates, fetchLimit, prefetching per research Pattern 5).
    7. Re-run all benchmarks to confirm fixes.
  </action>
  <verify>
    <automated>xcodebuild test -scheme Pault -destination 'platform=macOS' -configuration Release -only-testing PaultTests/PerformanceBenchmarkTests 2>&1 | tail -10</automated>
  </verify>
  <done>All performance benchmarks pass in Release config. Launch performance test runs successfully. Any bottlenecks identified are fixed with SwiftData or view body optimizations.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Instruments profiling session -- leaks and memory</name>
  <files>N/A -- manual Instruments sessions</files>
  <action>
    Human runs Instruments profiling sessions against the Release build. Performance benchmarks are now passing in Release config. The app is ready for manual Instruments profiling to verify zero leaks and stable memory.

    Run the following Instruments sessions:

    1. Leaks session (5 minutes):
       - Open Instruments (Xcode -> Open Developer Tool -> Instruments)
       - Select Leaks template, target Pault app (Release scheme)
       - Exercise: add 20+ blocks, switch between all 3 surfaces (main window, block editor, menu bar), trigger AI assist panel, run a prompt, navigate version history, open analytics
       - Check: Zero leaks in final snapshot
       - Pay attention to: Carbon GlobalHotkeyManager objects, SwiftData context references

    2. Allocations session (extended):
       - Instruments -> Allocations template
       - Exercise: Grow canvas to 20+ blocks, shrink back, navigate between prompts, open/close inspector
       - Check: Memory returns to baseline after shrinking. No unbounded growth.

    3. App Launch session:
       - Instruments -> App Launch template (Instruments 26)
       - Run 3 cold launches
       - Check: pre-main + main() combined < 1s

    Expected: Zero leaks, stable memory, launch < 1s.
  </action>
  <verify>
    <automated>echo "Manual Instruments verification -- human confirms zero leaks and stable memory"</automated>
  </verify>
  <done>Zero memory leaks in 5-minute editing session. Memory returns to baseline after canvas shrink. Launch time under 1s in Instruments App Launch. Human types "approved" to confirm.</done>
</task>

</tasks>

<verification>
- `xcodebuild test -only-testing PaultTests/PerformanceBenchmarkTests -configuration Release` passes
- `xcodebuild test -only-testing PaultUITests/PaultUITests/testLaunchPerformance -configuration Release` passes
- Manual Instruments sessions show zero leaks and stable memory
</verification>

<success_criteria>
- All 3 performance benchmarks pass their baselines in Release config
- Launch time < 1s confirmed via Instruments App Launch
- Zero memory leaks in 5-minute editing session
- Memory footprint stable (returns to baseline after shrinking canvas)
</success_criteria>

<output>
After completion, create `.planning/phases/08-final-quality-polish/08-02-SUMMARY.md`
</output>
