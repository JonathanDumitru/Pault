---
phase: 04-pro-features-ai-assist-api-runner
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - PaultTests/AIAssistPanelTests.swift
  - PaultTests/AIServiceTests.swift
  - PaultTests/PromptRunTests.swift
  - PaultTests/RunTabViewTests.swift
autonomous: true
requirements: [R2.1, R2.5, R5.1, R5.2, R5.3]

must_haves:
  truths:
    - "Test stubs exist for all Wave 0 gaps identified in VALIDATION.md"
    - "Each stub file compiles and contains placeholder test methods that fail with XCTFail or Issue.record"
    - "AIServiceTests expanded stubs cover proxy routing, 429 handling, and Ollama bypass"
  artifacts:
    - path: "PaultTests/AIAssistPanelTests.swift"
      provides: "Stub tests for snapshot-before-accept and streaming state management"
      min_lines: 30
    - path: "PaultTests/AIServiceTests.swift"
      provides: "Expanded stubs for proxy routing, rate limit parsing, Ollama bypass, streamImprove"
    - path: "PaultTests/PromptRunTests.swift"
      provides: "Expanded stubs for token metadata persistence"
    - path: "PaultTests/RunTabViewTests.swift"
      provides: "Stub tests for variable form pre-fill and Run Again behavior"
      min_lines: 30
  key_links: []
---

<objective>
Create Wave 0 test stubs for all test files identified as missing in VALIDATION.md.

Purpose: Nyquist compliance requires automated test files to exist before implementation tasks claim coverage. This plan creates the stub scaffolds so Plans 01-03 can reference concrete test targets in their verify commands.

Output: Four test files (new or expanded) with failing placeholder tests covering proxy routing, AI panel behavior, run persistence, and run tab interactions.
</objective>

<execution_context>
@/Users/dev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-pro-features-ai-assist-api-runner/04-CONTEXT.md
@.planning/phases/04-pro-features-ai-assist-api-runner/04-VALIDATION.md

<interfaces>
<!-- Existing test patterns from codebase -->

From PaultTests/AIServiceTests.swift (existing):
```swift
// XCTest-based tests for AIService actor
// Uses mock URLProtocol for network isolation
import XCTest
@testable import Pault

final class AIServiceTests: XCTestCase {
    // Existing tests for improve, suggestVariables, autoTag, qualityScore
    // Need expansion for: proxy routing, 429 handling, Ollama bypass, streamImprove
}
```

From PaultTests/PromptRunTests.swift (existing):
```swift
// Swift Testing framework
import Testing
@testable import Pault

// Existing tests for PromptRun model
// Need expansion for: token metadata persistence after proxy responses
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AIAssistPanelTests and RunTabViewTests stubs</name>
  <files>PaultTests/AIAssistPanelTests.swift, PaultTests/RunTabViewTests.swift</files>
  <action>
**Create PaultTests/AIAssistPanelTests.swift:**
New XCTest file with stub tests that fail immediately (XCTFail("Not yet implemented")):

1. `test_acceptImprove_callsSaveSnapshot` -- Verifies auto-snapshot is created before applying AI improve suggestion (R2.1)
2. `test_streamingState_showsCancelButton` -- Verifies isImproving state shows cancel button (R2.1)
3. `test_noKeyConfigured_showsSetupPrompt` -- Verifies locked state when no API key (R2.1)
4. `test_rateLimitError_showsRetryMessage` -- Verifies inline rate limit error display (R2.5)

Each test should have a descriptive comment explaining what it will verify once implemented. Use `XCTFail("Wave 0 stub -- implement in Plan 04-02")`.

**Create PaultTests/RunTabViewTests.swift:**
New Swift Testing file with stub tests:

1. `test_variableFormPreFillsDefaults` -- Verifies template variable defaults populate form fields (R5.1)
2. `test_runAgain_reExecutesWithSameInput` -- Verifies Run Again uses the original resolved input (R5.3)
3. `test_executeProGates_whenNotUnlocked` -- Verifies Pro feature gate on execute (R5.1)
4. `test_streamingCancel_stopsAccumulation` -- Verifies cancel stops token accumulation (R5.1)

Use `Issue.record("Wave 0 stub -- implement in Plan 04-03")` for Swift Testing framework stubs.
  </action>
  <verify>
    <automated>cd /Users/dev/Desktop/Software/macOS/Pault && xcodebuild build-for-testing -scheme Pault -destination 'platform=macOS' 2>&1 | tail -10</automated>
  </verify>
  <done>AIAssistPanelTests.swift and RunTabViewTests.swift exist with failing stub tests. Both files compile in the test target.</done>
</task>

<task type="auto">
  <name>Task 2: Expand AIServiceTests and PromptRunTests with proxy-related stubs</name>
  <files>PaultTests/AIServiceTests.swift, PaultTests/PromptRunTests.swift</files>
  <action>
**Expand PaultTests/AIServiceTests.swift:**
Add new stub test methods to the existing test class:

1. `test_proxyUnreachable_showsInlineError` -- Verifies graceful error when proxy is unreachable (R2.5). XCTFail stub.
2. `test_rateLimitResponse_parsesRetryAfter` -- Verifies 429 response parsing extracts Retry-After value (R2.5). XCTFail stub.
3. `test_ollamaBypassesProxy_directLocalhost` -- Verifies Ollama provider routes directly to localhost, not proxy (R2.5). XCTFail stub.
4. `test_streamImprove_returnsStreamEvents` -- Verifies streamImprove returns StreamEvent tokens (R2.1). XCTFail stub.
5. `test_claudeRoutesViaProxy_withHeaders` -- Verifies Claude requests include X-Provider, X-Provider-Key, X-Storekit-JWS headers (R2.5). XCTFail stub.
6. `test_qualityScoreReturnsTips` -- Verifies QualityScore includes tips array (R2.4). XCTFail stub.

Do NOT modify existing passing tests. Add new methods at the end of the class.

**Expand PaultTests/PromptRunTests.swift:**
Add new stub test methods:

1. `test_promptRunPersistsWithTokenMetadata` -- Verifies PromptRun stores inputTokens and outputTokens from proxy metadata (R5.2). Issue.record stub.
2. `test_promptRunStarRating_persistsOnReload` -- Verifies userRating persists across model context save/load (R5.2). Issue.record stub.

Do NOT modify existing passing tests. Add new test functions at the end of the file.
  </action>
  <verify>
    <automated>cd /Users/dev/Desktop/Software/macOS/Pault && xcodebuild build-for-testing -scheme Pault -destination 'platform=macOS' 2>&1 | tail -10</automated>
  </verify>
  <done>AIServiceTests.swift has 6 new stub methods for proxy routing, rate limiting, Ollama bypass, streaming, and tips. PromptRunTests.swift has 2 new stub methods for token metadata and star rating persistence. All stubs compile and fail with descriptive messages.</done>
</task>

</tasks>

<verification>
1. All 4 test files exist and compile in the PaultTests target
2. New stub tests fail with descriptive "Wave 0 stub" messages
3. Existing tests are not modified or broken
4. `xcodebuild build-for-testing` succeeds
</verification>

<success_criteria>
- PaultTests/AIAssistPanelTests.swift exists with 4 stub tests
- PaultTests/RunTabViewTests.swift exists with 4 stub tests
- PaultTests/AIServiceTests.swift expanded with 6 proxy-related stub tests
- PaultTests/PromptRunTests.swift expanded with 2 metadata stub tests
- All stubs compile and fail with descriptive messages
- No existing tests broken
</success_criteria>

<output>
After completion, create `.planning/phases/04-pro-features-ai-assist-api-runner/04-00-SUMMARY.md`
</output>
