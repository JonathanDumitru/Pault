---
phase: 01-compliance-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Pault/Pault.entitlements
  - Pault/PrivacyInfo.xcprivacy
  - Pault/PaultApp.swift
autonomous: false
requirements:
  - R7.1
  - R7.2

must_haves:
  truths:
    - "PrivacyInfo.xcprivacy declares UserDefaults (CA92.1) and FileTimestamp (C617.1) required-reason APIs"
    - "Apple-events temporary-exception entitlement is removed from Pault.entitlements"
    - "Only 3 justified entitlements remain: sandbox, network.client, files.user-selected.read-write"
    - "Paste migration code is removed from PaultApp.swift"
    - "App builds and runs without sandbox violations"
  artifacts:
    - path: "Pault/PrivacyInfo.xcprivacy"
      provides: "Privacy manifest with required-reason API declarations"
      contains: "NSPrivacyAccessedAPICategoryUserDefaults"
    - path: "Pault/Pault.entitlements"
      provides: "Cleaned entitlements with only justified entries"
      min_lines: 8
    - path: "Pault/PaultApp.swift"
      provides: "App entry point without stale migration code"
  key_links:
    - from: "Pault/PrivacyInfo.xcprivacy"
      to: "Pault app target"
      via: "Xcode target membership / Copy Bundle Resources"
      pattern: "NSPrivacyAccessedAPITypes"
---

<objective>
Create the PrivacyInfo.xcprivacy privacy manifest, remove the stale apple-events entitlement and paste migration code, and verify the app builds cleanly under sandbox.

Purpose: Eliminate the two hard-rejection blockers that would cause automatic App Store rejection (missing privacy manifest, unjustified entitlement).
Output: Clean entitlements file, privacy manifest in app bundle, no stale migration code.
</objective>

<execution_context>
@/Users/dev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-compliance-test-infrastructure/01-CONTEXT.md
@.planning/phases/01-compliance-test-infrastructure/01-RESEARCH.md

<interfaces>
<!-- Current entitlements file to be modified -->
From Pault/Pault.entitlements:
```xml
<!-- Contains 4 entries: app-sandbox, network.client, files.user-selected.read-write, temporary-exception.apple-events -->
<!-- The temporary-exception.apple-events entry must be removed entirely -->
```

From Pault/PaultApp.swift (lines 140-143 to remove):
```swift
// One-time migration: "paste" action removed in 2.5B -- fall back to "copy"
if UserDefaults.standard.string(forKey: "defaultAction") == "paste" {
    UserDefaults.standard.set("copy", forKey: "defaultAction")
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create privacy manifest and clean entitlements</name>
  <files>Pault/PrivacyInfo.xcprivacy, Pault/Pault.entitlements, Pault/PaultApp.swift</files>
  <action>
1. Create `Pault/PrivacyInfo.xcprivacy` with the following content (exact XML from research):
   - `NSPrivacyTracking`: false
   - `NSPrivacyTrackingDomains`: empty array
   - `NSPrivacyCollectedDataTypes`: empty array (Data Not Collected)
   - `NSPrivacyAccessedAPITypes`: two entries:
     - `NSPrivacyAccessedAPICategoryUserDefaults` with reason `CA92.1` (app preferences)
     - `NSPrivacyAccessedAPICategoryFileTimestamp` with reason `C617.1` (ErrorLogger log rotation within app container)

2. Before creating the file, check if `AttachmentManager.swift` accesses file timestamps outside the app container. Grep for `attributesOfItem`, `creationDate`, `modificationDate` in AttachmentManager.swift. If it accesses user-selected files (via NSOpenPanel), reason code C617.1 still covers app-managed data. If accessing files outside app container entirely, also add reason 3B52.1. Per research, C617.1 should suffice.

3. Edit `Pault/Pault.entitlements` to remove the entire `com.apple.security.temporary-exception.apple-events` entry (key + array value). Remove the comment above it. Keep the remaining 3 entries with their comments. Result should match:
   ```xml
   <?xml version="1.0" encoding="UTF-8"?>
   <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
   <plist version="1.0">
   <dict>
       <key>com.apple.security.app-sandbox</key>
       <true/>
       <key>com.apple.security.network.client</key>
       <true/>
       <key>com.apple.security.files.user-selected.read-write</key>
       <true/>
   </dict>
   </plist>
   ```

4. Edit `Pault/PaultApp.swift` to remove lines 140-143 (the paste-action migration block). The `init()` should end after `TemplateSeedService.seed(into: seedContext)` at line 138 (with closing brace).

5. Ensure `PrivacyInfo.xcprivacy` has correct Xcode target membership for the Pault app target. Check the `.xcodeproj/project.pbxproj` file to verify the new file is added to the Pault target's resources. If using `xcodebuild`, the file must be in the project's file references and the target's "Copy Bundle Resources" build phase.
  </action>
  <verify>
    <automated>xcodebuild build -project Pault.xcodeproj -scheme Pault -destination 'platform=macOS' 2>&1 | tail -5</automated>
  </verify>
  <done>
  - PrivacyInfo.xcprivacy exists with UserDefaults CA92.1 and FileTimestamp C617.1 declarations
  - Pault.entitlements contains exactly 3 entries (sandbox, network.client, files.user-selected.read-write)
  - No apple-events temporary exception in entitlements
  - PaultApp.swift has no paste migration code
  - Project builds successfully
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify sandbox compatibility after compliance changes</name>
  <files>N/A</files>
  <action>
  User verifies that the compliance changes do not break any sandbox-sensitive functionality.
  </action>
  <verify>User confirms no sandbox violations during manual testing</verify>
  <done>User has approved that hotkey paste, clipboard, and file access all work without sandbox violations</done>
  <what-built>Privacy manifest created, stale entitlement removed, paste migration code removed. The app should build and run without sandbox violations.</what-built>
  <how-to-verify>
    1. Open the built app (or run from Xcode)
    2. Exercise these sandbox-sensitive features:
       - Global hotkey paste (Cmd+Shift+Space or configured hotkey): verify paste into another app still works via CGEvent (not apple-events)
       - Copy a prompt to clipboard
       - File > Export a prompt (tests file access entitlement)
    3. Open Console.app, filter by "Pault", check for any sandbox violation messages
    4. Verify in Xcode: select PrivacyInfo.xcprivacy, confirm Target Membership shows "Pault" checked
    5. Run: `codesign -d --entitlements - build/Release/Pault.app 2>&1` and verify no apple-events exception appears
  </how-to-verify>
  <resume-signal>Type "approved" or describe any sandbox violations or issues</resume-signal>
</task>

</tasks>

<verification>
- Build succeeds with no warnings related to privacy or entitlements
- `codesign -d --entitlements -` on built app shows only 3 entitlements
- PrivacyInfo.xcprivacy is present in app bundle at Pault.app/Contents/Resources/PrivacyInfo.xcprivacy
- No sandbox violation logs in Console.app during basic usage
</verification>

<success_criteria>
- Privacy manifest exists and declares all required-reason APIs used by the app
- Entitlements are minimal and justified (no stale exceptions)
- App builds, runs, and passes basic functional testing without sandbox violations
</success_criteria>

<output>
After completion, create `.planning/phases/01-compliance-test-infrastructure/01-01-SUMMARY.md`
</output>
