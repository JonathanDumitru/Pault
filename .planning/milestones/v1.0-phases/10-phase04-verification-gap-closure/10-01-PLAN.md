---
phase: 10-phase04-verification-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Pault/RefinementLoopView.swift
  - .planning/phases/10-phase04-verification-gap-closure/10-VERIFICATION.md
  - .planning/phases/10-phase04-verification-gap-closure/10-01-SUMMARY.md
autonomous: true
requirements: [R2.1, R2.2, R2.3, R2.4, R2.5, R5.1, R5.2, R5.3]

must_haves:
  truths:
    - "R2.1 (AI Prompt Improvement) has concrete code evidence in AIAssistPanel.swift showing streaming rewrite with accept/reject and auto-snapshot"
    - "R2.2 (AI Variable Suggestion) has concrete code evidence showing per-suggestion accept/reject with saveSnapshot"
    - "R2.3 (AI Auto-Tagging) has concrete code evidence showing on-demand tag suggestion with accept/dismiss"
    - "R2.4 (AI Quality Scoring) has concrete code evidence showing four-axis score, visual bars, and tips"
    - "R2.5 (Proxy Service Integration) has concrete code evidence showing proxy routing for Claude/OpenAI, Ollama bypass, JWS auth, rate limit handling"
    - "R5.1 (Prompt Execution) has concrete code evidence showing Run tab with streaming display, model selection, cancel"
    - "R5.2 (Response Management) has concrete code evidence showing PromptRun persistence, history view, copy, star rating, delete"
    - "R5.3 (Refinement Loop) has concrete code evidence showing iteration history, DiffView, and auto-snapshot before accept"
    - "RefinementLoopView.accept() calls saveSnapshot before overwriting prompt.content"
    - "Phase 10 VERIFICATION.md exists with per-requirement verdicts and code evidence"
  artifacts:
    - path: ".planning/phases/10-phase04-verification-gap-closure/10-VERIFICATION.md"
      provides: "Per-requirement verification verdicts for R2.1-R2.5, R5.1-R5.3"
      contains: "status: passed"
    - path: "Pault/RefinementLoopView.swift"
      provides: "Fixed accept() with saveSnapshot call"
      contains: "saveSnapshot"
    - path: ".planning/phases/10-phase04-verification-gap-closure/10-01-SUMMARY.md"
      provides: "Phase 10 execution summary with requirements-completed"
      contains: "requirements"
  key_links:
    - from: "Pault/RefinementLoopView.swift"
      to: "Pault/Services/PromptService.swift"
      via: "saveSnapshot(for:source:) call in accept()"
      pattern: "saveSnapshot.*aiRefine"
---

<objective>
Fix the one real implementation gap in Phase 04 (missing saveSnapshot in RefinementLoopView.accept()) and produce a comprehensive VERIFICATION.md documenting code evidence for all 8 requirements (R2.1-R2.5, R5.1-R5.3).

Purpose: Close the verification gap identified by the milestone audit -- Phase 04 has no VERIFICATION.md and one implementation defect.
Output: Fixed RefinementLoopView.swift, VERIFICATION.md with per-requirement verdicts, SUMMARY.md
</objective>

<execution_context>
@/Users/dev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-phase04-verification-gap-closure/10-RESEARCH.md

@Pault/RefinementLoopView.swift
@Pault/AIAssistPanel.swift
@Pault/Services/AIService.swift
@Pault/Services/ProxyConfig.swift
@Pault/RunTabView.swift
@Pault/RunHistoryView.swift
@Pault/PromptDetailView.swift
@pault-proxy/src/index.ts
@PaultTests/AIServiceTests.swift
@PaultTests/PromptRunTests.swift
@PaultTests/RunTabViewTests.swift
@PaultTests/AIAssistPanelTests.swift
@.planning/phases/01-compliance-test-infrastructure/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix R5.3 saveSnapshot gap and verify all 8 requirements</name>
  <files>Pault/RefinementLoopView.swift</files>
  <action>
**Part A -- Fix the R5.3 implementation gap:**

In `Pault/RefinementLoopView.swift`, locate the `accept()` function (around line 195). Add a `saveSnapshot` call BEFORE `prompt.content = finalRevision` (line 231). The fix:

1. Add `PromptService(modelContext: modelContext).saveSnapshot(for: prompt, source: .aiRefine)` as the first line of the content-update block, immediately before `prompt.content = finalRevision`.
2. Confirm that `.aiRefine` exists on the VersionSource enum (check `Pault/Models/PromptVersion.swift` or similar). If it does not exist, use `.aiImprove` as the closest match and note this in VERIFICATION.md.
3. This matches the pattern used in all other AI accept paths (Improve tab's `acceptImprovement()`, Variables tab's `insert()`, Tags tab's `attachTag()`).

**Part B -- Verify all 8 requirements by code inspection:**

For each requirement, use grep/file reads to collect concrete evidence. Record file paths and line numbers. Specifically verify:

- **R2.1:** `AIService.streamImprove()` exists; `AIAssistPanel.runImprove()` streams tokens; `acceptImprovement()` calls `saveSnapshot` then sets `prompt.content`; `DiffView` shown after streaming.
- **R2.2:** `AIService.suggestVariables()` returns `[VariableSuggestion]`; `VariablesTabContent` has per-suggestion Accept/Reject; `insert()` calls `saveSnapshot`.
- **R2.3:** `AIService.autoTag()` returns `[String]`; `TagsTabContent` has "Suggest Tags" button; `attachTag()` calls `saveSnapshot`.
- **R2.4:** `AIService.qualityScore()` returns `QualityScore` with `tips` array; `ScoreTabContent` displays 4-axis ProgressView bars + tips list.
- **R2.5:** `buildRequest()` routes Claude/OpenAI through `ProxyConfig.baseURL` with `X-Provider`/`X-Storekit-JWS` headers; Ollama uses localhost directly; `handleAIError()` maps 401->subscriptionRequired, 429->rateLimited. Verify ProxyConfig.baseURL uses key `"ai.proxy.baseURL"` matching PreferencesView @AppStorage key (research flagged potential mismatch but actual code may match -- confirm).
- **R5.1:** `RunTabView.startRun()` calls `streamRun()`; token-by-token display with ScrollViewReader auto-scroll; cancel button; `DetailTab.run` wired in PromptDetailView.
- **R5.2:** `persistRun()` creates PromptRun with all fields; `RunHistoryView` fetches per-prompt runs; copy/star-rating/delete/Run Again all implemented.
- **R5.3:** `RefinementLoopView` has DiffView + star rating + Try Again; `tryAgain()` preserves iteration history; `accept()` persists all iterations as PromptRun records AND now calls saveSnapshot (after Part A fix).

Collect all evidence as structured notes for Task 2.
  </action>
  <verify>
    <automated>cd /Volumes/Drive/Projects/Software/macOS/Pault && grep -n "saveSnapshot" Pault/RefinementLoopView.swift | head -5 && xcodebuild test -scheme Pault -destination 'platform=macOS' -only-testing PaultTests/AIServiceTests 2>&1 | tail -20</automated>
  </verify>
  <done>RefinementLoopView.accept() calls saveSnapshot before prompt.content assignment. All 8 requirements have documented code evidence collected. AIServiceTests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Write VERIFICATION.md and SUMMARY.md</name>
  <files>.planning/phases/10-phase04-verification-gap-closure/10-VERIFICATION.md, .planning/phases/10-phase04-verification-gap-closure/10-01-SUMMARY.md</files>
  <action>
**Part A -- Write VERIFICATION.md:**

Create `.planning/phases/10-phase04-verification-gap-closure/10-VERIFICATION.md` following the Phase 01 VERIFICATION.md format exactly. Include:

1. **Frontmatter:**
   - `phase: 10-phase04-verification-gap-closure`
   - `verified: {ISO-8601 timestamp}`
   - `status: passed` (all gaps fixed)
   - `score: {N}/{N} must-haves verified`
   - `requirements_completed: [R2.1, R2.2, R2.3, R2.4, R2.5, R5.1, R5.2, R5.3]`
   - `human_verification:` section listing 3 items: AI streaming UI, proxy deployment, live API key execution (these cannot be automated)

2. **Observable Truths table:** One row per must-have truth from the 04-01/04-02/04-03 PLANs (29 truths total from RESEARCH.md). Status is VERIFIED/FAILED/UNCERTAIN with evidence column citing file:line.

3. **Required Artifacts table:** All files created/modified in Phase 04 (from RESEARCH.md "Files Created" and "Files Modified" sections). Status VERIFIED with line counts.

4. **Key Link Verification table:** Critical wiring connections:
   - AIService.buildRequest -> ProxyConfig.baseURL (proxy routing)
   - AIAssistPanel -> AIService.streamImprove (streaming)
   - acceptImprovement -> PromptService.saveSnapshot (auto-snapshot)
   - RefinementLoopView.accept -> PromptService.saveSnapshot (FIXED in Task 1)
   - RunTabView.startRun -> AIService.streamRun (execution)
   - RunTabView.persistRun -> PromptRun model (persistence)
   - ProStatusManager -> currentTransactionJWS (auth)

5. **Requirements Coverage table:** One row per R2.x/R5.x requirement with Source Plan, Description, Status (SATISFIED), and Evidence summary.

6. **Gaps Fixed section:** Document the R5.3 saveSnapshot fix applied in Task 1. Note that the R2.5 ProxyConfig key mismatch flagged by research was a false positive -- both ProxyConfig.swift and PreferencesView.swift use `"ai.proxy.baseURL"`.

7. **Manual Verification Items section:** Proxy deployment, live API key testing, AI streaming UI -- these require human action and cannot be automated.

**Part B -- Write SUMMARY.md:**

Create `.planning/phases/10-phase04-verification-gap-closure/10-01-SUMMARY.md` with standard SUMMARY frontmatter including:
- `requirements_completed: [R2.1, R2.2, R2.3, R2.4, R2.5, R5.1, R5.2, R5.3]`
- Key changes: R5.3 saveSnapshot fix, VERIFICATION.md creation
- Decisions made during execution
- Self-check confirming all 8 requirements have SATISFIED verdicts

**Part C -- Run full test suite as phase gate:**

Run `xcodebuild test -scheme Pault -destination 'platform=macOS' 2>&1 | tail -40` to confirm no regressions from the saveSnapshot fix.
  </action>
  <verify>
    <automated>cd /Volumes/Drive/Projects/Software/macOS/Pault && test -f .planning/phases/10-phase04-verification-gap-closure/10-VERIFICATION.md && test -f .planning/phases/10-phase04-verification-gap-closure/10-01-SUMMARY.md && grep -c "SATISFIED\|VERIFIED" .planning/phases/10-phase04-verification-gap-closure/10-VERIFICATION.md && xcodebuild test -scheme Pault -destination 'platform=macOS' 2>&1 | tail -20</automated>
  </verify>
  <done>10-VERIFICATION.md exists with all 8 requirements showing SATISFIED status and concrete code evidence. 10-01-SUMMARY.md exists with requirements-completed listing all 8 IDs. Full test suite passes green.</done>
</task>

</tasks>

<verification>
1. `grep -n "saveSnapshot" Pault/RefinementLoopView.swift` shows saveSnapshot call before prompt.content assignment
2. `10-VERIFICATION.md` exists with `status: passed` and all 8 requirement IDs in requirements_completed
3. `10-01-SUMMARY.md` exists with requirements_completed frontmatter
4. Full test suite passes with no regressions
</verification>

<success_criteria>
- RefinementLoopView.accept() calls saveSnapshot before prompt.content = finalRevision (R5.3 gap closed)
- VERIFICATION.md has per-requirement SATISFIED verdicts for all 8 requirements with file:line evidence
- ProxyConfig key mismatch confirmed as false positive (both use "ai.proxy.baseURL")
- Full test suite green
- SUMMARY.md documents all requirements as completed
</success_criteria>

<output>
After completion, create `.planning/phases/10-phase04-verification-gap-closure/10-01-SUMMARY.md`
</output>
