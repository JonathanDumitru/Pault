---
phase: 02-block-editor-polish
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - Pault/BlockEditor/Views/BlockRowView.swift
  - Pault/BlockEditor/Views/CompositionCanvasView.swift
  - Pault/BlockEditor/Views/BlockEditorView.swift
  - Pault/BlockEditor/Views/BlockLibraryView.swift
  - Pault/BlockEditor/Views/SlashCommandPaletteView.swift
  - Pault/BlockEditor/Views/CompiledPreviewView.swift
  - Pault/BlockEditor/Views/SuggestionBannerView.swift
  - Pault/BlockEditor/PromptStudioModel.swift
  - PaultTests/AccessibilityTests.swift
  - PaultTests/PerformanceBenchmarkTests.swift
autonomous: true
requirements: [R1.3, R1.4]

must_haves:
  truths:
    - "VoiceOver can navigate all canvas blocks and perform Move Up/Down, Delete, Duplicate via custom accessibility actions"
    - "VoiceOver announces block count changes ('Block added. 5 blocks on canvas.')"
    - "VoiceOver announces each block with category and position ('Role block: System Prompt, position 1 of 5')"
    - "Reduce Motion replaces spring animations with instant/cross-fade transitions"
    - "Auto-collapse is disabled when VoiceOver is active"
    - "Canvas remains responsive with 20+ blocks (no visible lag)"
    - "Compiled preview updates within 300ms of input change"
    - "Slash command palette opens in under 100ms"
    - "Visible focus ring (2pt system accent color border) on selected block"
    - "Placeholder status uses icons alongside color (Differentiate Without Color support)"
  artifacts:
    - path: "Pault/BlockEditor/Views/BlockRowView.swift"
      provides: "VoiceOver labels, custom actions, focus ring, Reduce Motion, Differentiate Without Color"
      contains: "accessibilityAction"
    - path: "PaultTests/AccessibilityTests.swift"
      provides: "Programmatic accessibility audit tests"
      min_lines: 100
    - path: "PaultTests/PerformanceBenchmarkTests.swift"
      provides: "XCTest measure() benchmarks for compilation, palette, 20+ blocks"
      min_lines: 80
  key_links:
    - from: "BlockRowView.swift"
      to: "PromptStudioModel.swift"
      via: "accessibilityAction buttons call model.moveBlock, model.removeFromCanvas, model.duplicateBlock"
      pattern: "accessibilityAction.*named"
    - from: "PromptStudioModel.swift"
      to: "AccessibilityNotification"
      via: "announceCanvasChange posts polite announcement after state changes"
      pattern: "AccessibilityNotification\\.Announcement"
    - from: "PerformanceBenchmarkTests.swift"
      to: "PromptStudioModel.swift"
      via: "measure { model.compileNow() } with 20+ blocks"
      pattern: "measure.*compileNow"
---

<objective>
Add full VoiceOver accessibility, Reduce Motion compliance, high contrast support, and performance benchmarks to the block editor.

Purpose: Accessibility is a locked decision with 25+ specific requirements from CONTEXT.md. Performance benchmarks establish baselines to prevent regressions. Together they ensure the editor is usable by everyone and stays fast at scale.

Output: Full VoiceOver navigation with custom actions, polite announcements, Reduce Motion compliance, Differentiate Without Color support, high contrast mode support, programmatic a11y audit tests, performance benchmarks for 20+ blocks/compilation/palette.
</objective>

<execution_context>
@/Users/dev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-block-editor-polish/02-CONTEXT.md
@.planning/phases/02-block-editor-polish/02-RESEARCH.md
@.planning/phases/02-block-editor-polish/02-01-SUMMARY.md
@.planning/phases/02-block-editor-polish/02-02-SUMMARY.md

<interfaces>
<!-- From Plans 02-01 and 02-02 outputs -->

From Pault/BlockEditor/Constants.swift (02-01):
```swift
enum AppConstants {
    enum Animation { static let standard, spring }
    enum Shadow { static let subtle, medium, elevated }
}
```

From Pault/BlockEditor/PromptStudioModel.swift (02-01 + 02-02):
```swift
weak var undoManager: UndoManager?
func addToCanvas(_ block: Block)               // registers undo + should announce
func removeFromCanvas(at offsets: IndexSet)     // registers undo + should announce
func moveOnCanvas(from:to:)                    // registers undo
func duplicateBlock(id: UUID)                  // registers undo + should announce
func insertOnCanvas(_ block: Block, at: Int)   // registers undo + should announce
func moveBlock(id: UUID, direction: Int)       // keyboard reorder
func compileNow()                               // must complete < 300ms for 20 blocks
```

From Pault/BlockEditor/Views/BlockRowView.swift (02-02):
- Has context menu, lift effect, drag handle
- Needs: .accessibilityLabel, .accessibilityActions, focus ring, Reduce Motion

From Pault/BlockEditor/Views/SlashCommandPaletteView.swift (02-02):
- Opens on Cmd+/ with fade+scale animation
- Needs: VoiceOver per-item announcements, selection tracking

From Pault/BlockEditor/Models/BlockPlaceholderStatus.swift:
- Enum with .filled, .partial, .empty cases
- Currently uses red/yellow/green colors only — needs icons for Differentiate Without Color
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: VoiceOver accessibility, Reduce Motion, high contrast, and Differentiate Without Color</name>
  <files>Pault/BlockEditor/Views/BlockRowView.swift, Pault/BlockEditor/Views/CompositionCanvasView.swift, Pault/BlockEditor/Views/BlockEditorView.swift, Pault/BlockEditor/Views/BlockLibraryView.swift, Pault/BlockEditor/Views/SlashCommandPaletteView.swift, Pault/BlockEditor/Views/CompiledPreviewView.swift, Pault/BlockEditor/Views/SuggestionBannerView.swift, Pault/BlockEditor/PromptStudioModel.swift</files>
  <action>
    **BlockRowView.swift — VoiceOver custom actions:**
    1. Add `.accessibilityLabel` with format: "{category} block: {title}, position {index+1} of {total}".
    2. Add `.accessibilityActions` with Button("Move Up"), Button("Move Down"), Button("Delete"), Button("Duplicate"), Button(isExpanded ? "Collapse" : "Expand").
    3. Make block elements separate navigable children (`.accessibilityElement(children: .contain)`) — not combined into single element.
    4. Add visible focus ring: when block is selected, show 2pt border in system accent color (`.overlay(RoundedRectangle().stroke(Color.accentColor, lineWidth: 2))`) — NOT a glow effect.
    5. Placeholder status indicators: alongside red/yellow/green colors, add SF Symbol icons — checkmark.circle for .filled, exclamationmark.triangle for .partial, xmark.circle for .empty. Use `@Environment(\.accessibilityDifferentiateWithoutColor)` to show icons always when that setting is on, or show them subtly alongside color always.
    6. Support high contrast mode: `@Environment(\.colorSchemeContrast)` — when `.increased`, use higher-opacity colors and thicker borders.

    **BlockRowView.swift — Reduce Motion:**
    7. Add `@Environment(\.accessibilityReduceMotion) var reduceMotion`.
    8. Replace all spring animations with `reduceMotion ? nil : .spring(response: 0.3, dampingFraction: 0.8)`.
    9. Expand/collapse: use `withAnimation(reduceMotion ? nil : .easeInOut(duration: 0.2))`.
    10. Apply Reduce Motion to lift effect, reflow animations, suggestion banner, palette opening.

    **CompositionCanvasView.swift — Announcements:**
    11. Add `import Accessibility` at top.
    12. After addToCanvas/removeFromCanvas/insertOnCanvas: post polite announcement with `DispatchQueue.main.asyncAfter(deadline: .now() + 0.1)` per research pattern:
        - Add: "Block added. {count} blocks on canvas."
        - Remove: "Block removed. {count} blocks on canvas."
    13. Tab order: ensure Library > Canvas (blocks > inputs) > Preview order via view hierarchy arrangement and `.accessibilityElement(children: .contain)` on panels.
    14. Announce suggestion banner as polite alert.
    15. Panel expand/collapse state announced.
    16. Token estimate and warnings as part of canvas accessibility summary.

    **BlockLibraryView.swift:**
    17. Add `.accessibilityAction(named: "Add to Canvas")` on each library block item.

    **SlashCommandPaletteView.swift:**
    18. Add per-item VoiceOver announcements and selection tracking.
    19. Ensure full keyboard navigation within palette (up/down arrows, enter to select, esc to dismiss).

    **CompiledPreviewView.swift:**
    20. Polite VoiceOver announcements for preview updates (use `.low` priority so it doesn't interrupt block navigation).

    **BlockEditorView.swift:**
    21. Add `@Environment(\.accessibilityVoiceOverEnabled) var voiceOverEnabled`.
    22. Disable auto-collapse when VoiceOver is active (pass voiceOverEnabled to any auto-collapse logic).
    23. Keyboard shortcut guide: add floating panel triggered by Cmd+? (or ? button) listing all shortcuts. Accessible as help rotor item.
    24. Modifier picker: when open, trap VoiceOver focus (modal behavior).

    **PromptStudioModel.swift:**
    25. Add `func announceCanvasChange(_ message: String)` helper that posts `AccessibilityNotification.Announcement(message).post()` with 0.1s delay.
    26. Validation errors: announce on field exit (not live) — post announcement when user tabs away from a field with validation error.

    **SuggestionBannerView.swift:**
    27. Animate with slide + fade (respect Reduce Motion).
    28. Mark as polite accessibility alert.
  </action>
  <verify>
    <automated>xcodebuild build -project Pault.xcodeproj -scheme Pault -destination 'platform=macOS' 2>&1 | tail -5</automated>
  </verify>
  <done>Every BlockRowView has accessibilityLabel with category/position and 5 custom accessibilityActions. Polite announcements fire after canvas state changes. Reduce Motion disables all spring/slide animations. Auto-collapse disabled when VoiceOver active. Differentiate Without Color shows icons alongside status colors. High contrast mode increases color opacity. Library blocks have "Add to Canvas" accessibility action. Keyboard shortcut help panel accessible. Project builds without errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Performance benchmarks, programmatic accessibility audit tests, and snapshot test setup</name>
  <files>PaultTests/AccessibilityTests.swift, PaultTests/PerformanceBenchmarkTests.swift, PaultTests/CanvasSnapshotTests.swift</files>
  <behavior>
    - Test: BlockRowView has non-empty accessibilityLabel
    - Test: BlockRowView has accessibilityActions for Move Up, Move Down, Delete, Duplicate
    - Test: PromptStudioModel with 20 blocks compiles within performance baseline
    - Test: SlashCommandState filter executes within performance baseline
    - Test: Canvas snapshot matches reference for empty state
    - Test: Canvas snapshot matches reference for multi-block state
    - Test: Canvas snapshot matches reference for dark mode
  </behavior>
  <action>
    **PaultTests/AccessibilityTests.swift:**
    1. Create programmatic accessibility audit tests that verify:
       - BlockRowView accessibility labels contain category and position
       - BlockRowView has expected accessibility actions (move up, move down, delete, duplicate, expand/collapse)
       - Placeholder status indicators have accessibility labels
       - Canvas summary includes block count
       - These tests work at the model/view-model level where possible (verifying that accessibility strings are generated correctly), not requiring full UI hosting

    **PaultTests/PerformanceBenchmarkTests.swift (XCTestCase for measure{}):**
    1. `testCompilationPerformanceWith20Blocks`: Create a PromptStudioModel with 20 blocks, all with filled inputs. `measure { model.compileNow() }`. Baseline must be < 300ms.
    2. `testPaletteFilterPerformance`: Seed SlashCommandState with all available blocks. `measure { state.filter(query: "role") }`. Baseline must be < 10ms (palette must open < 100ms including UI).
    3. `testCanvasRenderPerformanceWith20Blocks`: If feasible, measure adding 20 blocks sequentially to verify no O(n^2) degradation.
    4. Use TestHelpers.makeTestModelContext() for SwiftData setup where needed.

    **PaultTests/CanvasSnapshotTests.swift (XCTestCase for swift-snapshot-testing):**
    1. Add swift-snapshot-testing package dependency if not already present (https://github.com/pointfreeco/swift-snapshot-testing, Up To Next Major from 1.17.0).
    2. Create snapshot tests using NSHostingController pattern from research:
       - testEmptyCanvas (light mode)
       - testSingleBlockCanvas
       - testMultiBlockCanvas (3-5 blocks)
       - testEmptyCanvasDarkMode
       - testMultiBlockCanvasDarkMode
       - testExpandedBlockState
       - testBlockWithValidationError
    3. Set explicit frame sizes (500x600) for deterministic snapshots.
    4. First run creates reference images (record mode). Mark these as optional for CI per testing strategy decision.
    5. Note: snapshot tests may need a test window on macOS for layout — use the NSWindow pattern from research pitfall 3 if needed.
  </action>
  <verify>
    <automated>xcodebuild test -project Pault.xcodeproj -scheme Pault -only-testing PaultTests/AccessibilityTests -only-testing PaultTests/PerformanceBenchmarkTests -destination 'platform=macOS' 2>&1 | tail -10</automated>
  </verify>
  <done>AccessibilityTests has 4+ tests verifying VoiceOver labels and actions programmatically. PerformanceBenchmarkTests has 2+ XCTest measure() benchmarks (compilation < 300ms, palette filter < 10ms). CanvasSnapshotTests has 7 snapshot test cases (reference images generated on first run). All tests pass. All existing tests still pass.</done>
</task>

</tasks>

<verification>
- All existing tests pass (no regressions from 02-01 and 02-02)
- AccessibilityTests pass (4+ test cases)
- PerformanceBenchmarkTests pass with baselines under thresholds
- CanvasSnapshotTests generate reference images on first run
- VoiceOver custom actions present on every BlockRowView
- Reduce Motion disables spring animations
- Project builds and runs without errors
</verification>

<success_criteria>
- VoiceOver can navigate all canvas elements and perform all operations via custom actions
- VoiceOver announces canvas state changes with polite priority
- Reduce Motion replaces all spring/slide animations with instant transitions
- Auto-collapse disabled when VoiceOver active
- Differentiate Without Color shows icons alongside status colors
- Canvas responsive with 20+ blocks (compilation < 300ms, palette < 100ms)
- Memory footprint stable during extended editing (no leaks in benchmark)
- Snapshot tests cover key visual states in light and dark mode
</success_criteria>

<output>
After completion, create `.planning/phases/02-block-editor-polish/02-03-SUMMARY.md`
</output>
