---
phase: 09-privacyinfo-xcode-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/09-privacyinfo-xcode-wiring/09-01-SUMMARY.md
  - .planning/phases/09-privacyinfo-xcode-wiring/09-VERIFICATION.md
autonomous: true
requirements:
  - R7.1
gap_closure: true

must_haves:
  truths:
    - "PrivacyInfo.xcprivacy is present in the built Pault.app bundle at Contents/Resources/PrivacyInfo.xcprivacy"
    - "No PBXFileSystemSynchronizedBuildFileExceptionSet excludes PrivacyInfo.xcprivacy from the build"
    - "PrivacyInfo.xcprivacy has valid plist format with CA92.1 and C617.1 reason codes"
    - "R7.1 integration gap from Phase 1 VERIFICATION is formally closed with evidence"
  artifacts:
    - path: ".planning/phases/09-privacyinfo-xcode-wiring/09-VERIFICATION.md"
      provides: "Formal verification closing the Phase 1 gap"
      contains: "R7.1"
    - path: ".planning/phases/09-privacyinfo-xcode-wiring/09-01-SUMMARY.md"
      provides: "Plan execution summary with evidence"
      contains: "requirements-completed"
  key_links:
    - from: "Pault/PrivacyInfo.xcprivacy"
      to: "Pault.app/Contents/Resources/PrivacyInfo.xcprivacy"
      via: "PBXFileSystemSynchronizedRootGroup auto-inclusion"
      pattern: "PBXFileSystemSynchronizedRootGroup"
---

<objective>
Verify that PrivacyInfo.xcprivacy is correctly bundled into the Pault app via Xcode 16's PBXFileSystemSynchronizedRootGroup mechanism, and formally close the R7.1 integration gap from Phase 1 VERIFICATION.

Purpose: The Phase 1 verifier flagged PrivacyInfo.xcprivacy as "not wired" because it found 0 references in project.pbxproj. Research confirmed this was a false positive — the project uses Xcode 16 folder synchronization which auto-includes all files without explicit PBXFileReference entries. DerivedData inspection (April 20, 2026) already confirmed the file IS in the bundle. This plan performs a fresh build verification and produces the formal VERIFICATION.md to close the gap.

Output: 09-VERIFICATION.md with per-requirement verdicts, 09-01-SUMMARY.md
</objective>

<execution_context>
@/Users/dev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-privacyinfo-xcode-wiring/09-RESEARCH.md

Key facts from research:
- Project uses PBXFileSystemSynchronizedRootGroup (Xcode 16 folder sync) — objectVersion 77
- PBXResourcesBuildPhase has empty files = () — this is CORRECT for synced-group projects
- No PBXFileSystemSynchronizedBuildFileExceptionSet entries exist in project.pbxproj
- DerivedData from April 20, 2026 already shows PrivacyInfo.xcprivacy at Pault.app/Contents/Resources/
- DO NOT add explicit PBXFileReference or PBXBuildFile entries — this would cause "Multiple commands produce" build errors
- DO NOT modify project.pbxproj in any way — the synchronized group handles everything automatically
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify PrivacyInfo.xcprivacy bundling and project structure</name>
  <files>.planning/phases/09-privacyinfo-xcode-wiring/09-01-SUMMARY.md</files>
  <action>
Run the following verification checks in sequence. Capture all output as evidence for the VERIFICATION.md.

1. Validate xcprivacy content:
   ```
   plutil -lint Pault/PrivacyInfo.xcprivacy
   plutil -p Pault/PrivacyInfo.xcprivacy
   ```
   Expected: OK format, contains NSPrivacyAccessedAPICategoryUserDefaults with CA92.1 and NSPrivacyAccessedAPICategoryFileTimestamp with C617.1.

2. Confirm PBXFileSystemSynchronizedRootGroup covers Pault/:
   ```
   grep "PBXFileSystemSynchronizedRootGroup" Pault.xcodeproj/project.pbxproj
   ```
   Expected: Entry with path = Pault.

3. Confirm no exception sets exclude the file:
   ```
   grep -c "membershipExceptions\|PBXFileSystemSynchronizedBuildFileExceptionSet" Pault.xcodeproj/project.pbxproj
   ```
   Expected: 0 (no exclusions configured).

4. Clean build and verify bundle:
   ```
   xcodebuild clean build -project Pault.xcodeproj -scheme Pault -configuration Debug -destination 'platform=macOS' 2>&1 | tail -5
   ```
   Then find and inspect the built bundle:
   ```
   find ~/Library/Developer/Xcode/DerivedData -name "Pault.app" -path "*/Debug/*" -maxdepth 12 | head -1 | xargs -I{} find {} -name "PrivacyInfo.xcprivacy"
   ```
   Expected: Path ending in Pault.app/Contents/Resources/PrivacyInfo.xcprivacy.

5. Verify xcprivacy content in the BUILT bundle (not just source):
   ```
   find ~/Library/Developer/Xcode/DerivedData -name "Pault.app" -path "*/Debug/*" -maxdepth 12 | head -1 | xargs -I{} plutil -p {}/Contents/Resources/PrivacyInfo.xcprivacy
   ```
   Expected: Same CA92.1 and C617.1 declarations as source.

Record all command outputs. If any check fails, investigate before proceeding — do NOT modify project.pbxproj.
  </action>
  <verify>
    <automated>plutil -lint Pault/PrivacyInfo.xcprivacy && grep -q "PBXFileSystemSynchronizedRootGroup" Pault.xcodeproj/project.pbxproj && test "$(grep -c 'membershipExceptions' Pault.xcodeproj/project.pbxproj)" = "0" && echo "All pre-build checks passed"</automated>
  </verify>
  <done>All 5 verification checks pass: xcprivacy is valid, synced group covers Pault/, no exclusions exist, clean build succeeds, and PrivacyInfo.xcprivacy is present at Pault.app/Contents/Resources/PrivacyInfo.xcprivacy in the built bundle with correct content</done>
</task>

<task type="auto">
  <name>Task 2: Write VERIFICATION.md closing R7.1 integration gap</name>
  <files>.planning/phases/09-privacyinfo-xcode-wiring/09-VERIFICATION.md</files>
  <action>
Create `.planning/phases/09-privacyinfo-xcode-wiring/09-VERIFICATION.md` with the standard verification report format:

1. Set frontmatter: phase 09, status verified, score 4/4 must-haves.

2. Observable Truths table (4 rows):
   - PrivacyInfo.xcprivacy present in built bundle at Contents/Resources/ — VERIFIED with build evidence from Task 1
   - No exception sets exclude the file — VERIFIED with grep evidence
   - xcprivacy has valid format with CA92.1 + C617.1 — VERIFIED with plutil evidence
   - R7.1 integration gap formally closed — VERIFIED

3. Requirements Coverage section:
   - R7.1: Privacy & Compliance — VERIFIED — include all evidence from Task 1

4. Phase 1 Gap Resolution section explaining:
   - The Phase 1 VERIFICATION gap (01-VERIFICATION.md truth #6) was a false positive
   - The verifier checked for PBXFileReference/PBXBuildFile entries, which is the correct check for classic Xcode projects
   - This project uses PBXFileSystemSynchronizedRootGroup (Xcode 16), which auto-includes all folder files without explicit project.pbxproj entries
   - Empty PBXResourcesBuildPhase files = () is correct behavior for synchronized-group projects
   - Ground truth is the built bundle, which contains the file

5. Key takeaway: No code changes were needed. The file was always being bundled correctly.

Use exact command outputs from Task 1 as evidence throughout.
  </action>
  <verify>
    <automated>test -f .planning/phases/09-privacyinfo-xcode-wiring/09-VERIFICATION.md && grep -q "R7.1" .planning/phases/09-privacyinfo-xcode-wiring/09-VERIFICATION.md && grep -q "VERIFIED" .planning/phases/09-privacyinfo-xcode-wiring/09-VERIFICATION.md && echo "VERIFICATION.md exists with R7.1 verdicts"</automated>
  </verify>
  <done>09-VERIFICATION.md exists with status: verified, all 4 must-haves marked VERIFIED, R7.1 marked VERIFIED with build evidence, and Phase 1 gap resolution explanation documenting the false-positive finding</done>
</task>

</tasks>

<verification>
Phase 9 is complete when:
1. Fresh xcodebuild clean build succeeds
2. PrivacyInfo.xcprivacy found at Pault.app/Contents/Resources/ in built bundle
3. plutil confirms valid format with CA92.1 and C617.1 reason codes
4. No exclusion sets exist in project.pbxproj
5. 09-VERIFICATION.md exists with R7.1 VERIFIED and all evidence documented
</verification>

<success_criteria>
- PrivacyInfo.xcprivacy confirmed in built app bundle via clean build evidence
- R7.1 integration gap from Phase 1 VERIFICATION formally closed
- 09-VERIFICATION.md documents that no project.pbxproj changes were needed (PBXFileSystemSynchronizedRootGroup handles inclusion automatically)
- Zero files modified in the codebase — this is a verification-only phase
</success_criteria>

<output>
After completion, create `.planning/phases/09-privacyinfo-xcode-wiring/09-01-SUMMARY.md`
</output>
