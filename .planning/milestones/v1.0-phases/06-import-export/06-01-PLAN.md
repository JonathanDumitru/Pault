---
phase: 06-import-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Pault/ExportService.swift
  - Pault/MarkdownFrontmatterParser.swift
  - PaultTests/ExportServiceTests.swift
  - PaultTests/MarkdownFrontmatterParserTests.swift
autonomous: true
requirements: [R8.1, R8.2]

must_haves:
  truths:
    - "User can export entire library or collection-scoped prompts as JSON v2 with all metadata fields"
    - "User can export prompts as individual Markdown files with YAML frontmatter into a chosen folder"
    - "User can copy a prompt as Markdown (with frontmatter) to clipboard"
    - "JSON v1 bundles import cleanly with new optional fields defaulting to nil"
    - "Markdown round-trips preserve title, tags, favorite, archived, dates, quality score, and variables"
  artifacts:
    - path: "Pault/ExportService.swift"
      provides: "v2 DTOs, JSON export, Markdown export, Copy as Markdown, buildRecord helper"
      contains: "PromptExportBundle"
    - path: "Pault/MarkdownFrontmatterParser.swift"
      provides: "YAML frontmatter serialization and parsing"
      exports: ["serialize", "parse"]
    - path: "PaultTests/ExportServiceTests.swift"
      provides: "v2 round-trip, v1 backward compat, slug collision, Copy as Markdown tests"
      min_lines: 80
    - path: "PaultTests/MarkdownFrontmatterParserTests.swift"
      provides: "Serialize-parse round-trip, plain Markdown fallback, colon-in-title edge case"
      min_lines: 60
  key_links:
    - from: "Pault/ExportService.swift"
      to: "Pault/MarkdownFrontmatterParser.swift"
      via: "exportMarkdown calls MarkdownFrontmatterParser.serialize"
      pattern: "MarkdownFrontmatterParser\\.serialize"
    - from: "Pault/ExportService.swift"
      to: "Prompt model"
      via: "buildRecord reads all v2 fields from Prompt"
      pattern: "prompt\\.blockCompositionData|prompt\\.qualityScore|prompt\\.lastUsedAt"
---

<objective>
Build the export layer and data format foundation for Phase 6: v2 JSON DTOs with all new metadata fields, Markdown frontmatter serializer/parser, JSON and Markdown export providers, Copy as Markdown clipboard action, and comprehensive unit tests.

Purpose: All export functionality and data format contracts must exist before Plan 02 can build the import system (which reuses the same DTOs and parser).
Output: Extended ExportService.swift, new MarkdownFrontmatterParser.swift, and test files proving round-trip fidelity.
</objective>

<execution_context>
@/Users/dev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-import-export/06-CONTEXT.md
@.planning/phases/06-import-export/06-RESEARCH.md

@Pault/ExportService.swift
@Pault/MarkdownFrontmatterParser.swift
@Pault/Prompt.swift
@Pault/TemplateEngine.swift
@PaultTests/TestHelpers.swift

<interfaces>
<!-- Existing ExportService pattern (to be extended) -->
From Pault/ExportService.swift:
```swift
enum ExportService {
    @discardableResult static func exportAll(prompts: [Prompt]) -> Bool
    @discardableResult static func importPrompts(into context: ModelContext) -> Int?
    private static func resolveTag(named name: String, in context: ModelContext) -> Tag
}
struct PromptExportBundle: Codable { let version: Int; let exportedAt: Double; let prompts: [PromptExportRecord] }
struct PromptExportRecord: Codable { /* v1 fields only — needs v2 extension */ }
struct VariableExportRecord: Codable { let name: String; let defaultValue: String; let sortOrder: Int }
```

From Pault/DiffEngine.swift:
```swift
enum DiffEngine {
    static func diff(old: String, new: String) -> [LineDiff]
    static func characterDiff(old: String, new: String) -> [CharacterDiff]
}
```

From Pault/TemplateEngine.swift:
```swift
enum TemplateEngine {
    static func extractVariableNames(from content: String) -> [String]
    static func resolve(content: String, variables: [TemplateVariable]) -> String
}
```

From Pault/PromptService.swift:
```swift
func saveSnapshot(for prompt: Prompt, changeNote: String? = nil, source: VersionSource = .manual, limit: Int = 50)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: v2 DTOs, MarkdownFrontmatterParser, and unit tests</name>
  <files>Pault/MarkdownFrontmatterParser.swift, PaultTests/ExportServiceTests.swift, PaultTests/MarkdownFrontmatterParserTests.swift</files>
  <behavior>
    - ExportServiceTests: PromptExportRecord v2 encodes all new optional fields (blockCompositionData, qualityScore, lastUsedAt, editingModeRaw, variantB, attachmentFileNames) and decodes them back
    - ExportServiceTests: v1 JSON bundle (missing v2 fields) decodes into PromptExportRecord with nil optionals — no errors
    - ExportServiceTests: PromptExportBundle v2 includes collectionName field
    - ExportServiceTests: slugify produces lowercase-alphanumeric-hyphen filenames, collision dedup appends -2, -3
    - MarkdownFrontmatterParserTests: serialize produces valid YAML frontmatter with quoted title, inline tag array, ISO8601 dates, optional quality_score, variable block sequence
    - MarkdownFrontmatterParserTests: parse round-trips serialize output back to matching record fields
    - MarkdownFrontmatterParserTests: parse handles plain Markdown (no frontmatter) — title from first H1, fallback to filename
    - MarkdownFrontmatterParserTests: parse handles title containing colon (e.g. "URL: https://example.com") correctly
    - MarkdownFrontmatterParserTests: parse handles empty tags array and missing optional fields
  </behavior>
  <action>
    1. Create `Pault/MarkdownFrontmatterParser.swift` as a new file with:
       - `struct MarkdownImportRecord` with fields: title, content, tags, isFavorite, isArchived, createdAt, updatedAt, qualityScore, variables (array of name+defaultValue tuples), id (optional String for UUID round-trip)
       - `static func serialize(record: PromptExportRecord) -> String` — YAML frontmatter with `---` delimiters, always-quoted string values for title, ISO8601 dates, inline `[tag1, tag2]` array, optional quality_score, block sequence for variables. Body: `# Title\n\ncontent`
       - `static func parse(markdown: String, filename: String) -> MarkdownImportRecord` — detects `---` frontmatter delimiters, parses key-value lines with support for quoted strings, inline arrays `[a, b]`, block sequences for variables. Falls back to `parsePlain` if no frontmatter found.
       - `static func parsePlain(markdown: String, filename: String) -> MarkdownImportRecord` — title from first `# Heading`, fallback to filename minus `.md` extension. Content is the full body.
       - `static func slugify(_ title: String, existing: Set<String>) -> String` — lowercase, replace non-alphanumeric with hyphens, collapse consecutive hyphens, trim leading/trailing hyphens. On collision append `-2`, `-3`, etc.
       - Always quote string values in YAML output to handle colons, brackets, hashes safely (per RESEARCH pitfall 1).

    2. Create `PaultTests/MarkdownFrontmatterParserTests.swift` (XCTestCase). Write failing tests first:
       - `testSerializeRoundTrip` — serialize a record, parse it back, assert all fields match
       - `testParsePlainMarkdown` — no frontmatter, title from H1
       - `testParsePlainMarkdownFallbackToFilename` — no frontmatter, no H1, title from filename
       - `testParseQuotedTitleWithColon` — title: "URL: https://example.com" round-trips
       - `testParseEmptyTags` — tags: [] parses to empty array
       - `testParseMissingOptionalFields` — no quality_score line, no variables block

    3. Create `PaultTests/ExportServiceTests.swift` (XCTestCase). Write failing tests first:
       - `testV2RecordEncodeDecode` — encode PromptExportRecord with all v2 fields, decode, assert equality
       - `testV1BundleDecodesAsV2` — create v1-shaped JSON (no v2 keys), decode as PromptExportRecord, assert v2 optionals are nil
       - `testBundleV2IncludesCollectionName` — encode/decode PromptExportBundle with collectionName
       - `testSlugify` — "Hello World!" -> "hello-world", collision dedup
       - `testSlugifyCollision` — two identical titles get "-2" suffix on second

    4. Make all tests pass by implementing MarkdownFrontmatterParser and updating DTOs (Task 2 handles DTO changes in ExportService.swift).

    Note: The v2 DTO definitions belong in ExportService.swift and will be written in Task 2. For this task, define a minimal PromptExportRecord extension or test-local struct to verify encode/decode behavior. If test compilation requires the actual v2 struct, write both tasks' code together but keep the TDD discipline (tests first, then make green).
  </action>
  <verify>
    <automated>cd /Volumes/Drive/Projects/Software/macOS/Pault && xcodebuild test -scheme Pault -only-testing:PaultTests/MarkdownFrontmatterParserTests -only-testing:PaultTests/ExportServiceTests -destination 'platform=macOS' 2>&1 | tail -20</automated>
  </verify>
  <done>All MarkdownFrontmatterParser and ExportService DTO tests pass. Markdown serialize-parse round-trip preserves all fields. v1 JSON decodes cleanly into v2 structs. Slug collision dedup works.</done>
</task>

<task type="auto">
  <name>Task 2: Extend ExportService with v2 JSON export, Markdown export, and Copy as Markdown</name>
  <files>Pault/ExportService.swift</files>
  <action>
    1. Update `PromptExportBundle` to v2:
       - Add `collectionName: String?` field (nil for library-wide)
       - Keep backward compatibility: `collectionName` defaults via `init` or CodingKeys

    2. Update `PromptExportRecord` to v2 — add Optional fields:
       - `blockCompositionData: Data?`
       - `qualityScore: Int?`
       - `lastUsedAt: Double?`
       - `editingModeRaw: String?`
       - `variantB: String?`
       - `attachmentFileNames: [String]?`
       All new fields are Optional so JSONDecoder handles v1 bundles (missing keys decode as nil).

    3. Add `static func buildRecord(from prompt: Prompt) -> PromptExportRecord` helper that maps all Prompt fields including new v2 ones (blockCompositionData, qualityScore, lastUsedAt, editingModeRaw, variantB, attachmentFileNames from prompt.attachments).

    4. Refactor existing `exportAll` to use `buildRecord` internally. Keep the method signature but have it produce version: 2 bundles.

    5. Add `static func exportLibraryJSON(prompts: [Prompt], collectionName: String? = nil) -> Bool`:
       - Uses `buildRecord` to create v2 records
       - Creates PromptExportBundle with version: 2, collectionName
       - NSSavePanel with title "Export Prompts", default filename "pault-prompts.json" (or collection-name slug if collection-scoped)
       - Pretty-printed, sorted keys

    6. Add `static func exportMarkdown(prompts: [Prompt]) -> Bool`:
       - Uses NSOpenPanel as folder picker (canChooseDirectories=true, canChooseFiles=false, canCreateDirectories=true) per RESEARCH pitfall 2
       - Iterates prompts, calls `buildRecord` then `MarkdownFrontmatterParser.serialize`
       - Uses `MarkdownFrontmatterParser.slugify` with collision tracking via `Set<String>`
       - Writes each `.md` file atomically
       - Logs count of written files

    7. Add `static func copyAsMarkdown(prompt: Prompt) -> Bool`:
       - Builds a PromptExportRecord via `buildRecord`
       - Calls `MarkdownFrontmatterParser.serialize` to produce Markdown with YAML frontmatter
       - Writes to `NSPasteboard.general` as `.string` type
       - Returns true on success

    8. Update `exportAll` to internally call `exportLibraryJSON` (or just produce v2 bundles directly) so PreferencesView call site continues working without changes.

    Keep `importPrompts(into:)` and `resolveTag` as-is for now — Plan 02 will replace import logic.
  </action>
  <verify>
    <automated>cd /Volumes/Drive/Projects/Software/macOS/Pault && xcodebuild test -scheme Pault -only-testing:PaultTests/ExportServiceTests -only-testing:PaultTests/MarkdownFrontmatterParserTests -destination 'platform=macOS' 2>&1 | tail -20</automated>
  </verify>
  <done>ExportService produces v2 JSON bundles with all metadata fields. Markdown export writes one .md file per prompt with YAML frontmatter to a user-chosen folder. Copy as Markdown writes frontmatter+body to clipboard. All existing tests plus new tests pass. Existing PreferencesView call site continues working.</done>
</task>

</tasks>

<verification>
1. All ExportServiceTests pass (v2 encode/decode, v1 backward compat, slug dedup)
2. All MarkdownFrontmatterParserTests pass (round-trip, plain Markdown, colon edge case)
3. Full test suite: `xcodebuild test -scheme Pault -destination 'platform=macOS'` — no regressions
4. ExportService.exportAll still works (PreferencesView backward compat)
</verification>

<success_criteria>
- PromptExportRecord v2 includes blockCompositionData, qualityScore, lastUsedAt, editingModeRaw, variantB, attachmentFileNames as optionals
- v1 JSON bundles decode into v2 structs with nil optionals (no errors)
- MarkdownFrontmatterParser serializes and parses YAML frontmatter with full round-trip fidelity
- Plain Markdown (no frontmatter) parses title from H1 or filename fallback
- Markdown filename slugification handles collisions with -2/-3 suffixes
- Copy as Markdown writes frontmatter + body to NSPasteboard
- All unit tests green, no regressions in existing suite
</success_criteria>

<output>
After completion, create `.planning/phases/06-import-export/06-01-SUMMARY.md`
</output>
