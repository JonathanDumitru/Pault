---
milestone: v1.1
checked: 2026-04-29
phases: [13, 14, 15, 16, 17]
status: passed
---

# v1.1 Integration Check

**Milestone:** Tech Debt Cleanup (Phases 13–17)
**Checked:** 2026-04-29
**Verdict:** ALL FLOWS WIRED — 0 broken flows, 0 orphaned exports, 0 missing connections

---

## Summary

**Connected:** 12/12 requirement-level cross-phase wiring points verified at source
**Orphaned:** 0 exports created but unused
**Missing:** 0 expected connections not found

**API Coverage:** N/A — v1.1 is a native macOS app (no HTTP routes); all data flows are Swift call chains
**Auth Protection:** N/A — no auth routes modified in v1.1

**E2E Flows Complete:** 5
**E2E Flows Broken:** 0

---

## Verified Flows

### Flow 1 — testShot06_AnalyticsDashboard E2E (TEST-01 + UX-03)

**Steps verified at source:**

1. `PaultUITests/ScreenshotTests.swift:64` — `app.launchArguments = ["--screenshot-mode", ...]` injects the argument before launch.
2. `Pault/Services/ProStatusManager.swift:28-32` — `init()` contains a `#if DEBUG` block that runs `if ProcessInfo.processInfo.arguments.contains("--screenshot-mode") { isProUnlocked = true }` **before** line 33 (`Task { await refreshStatus() }`). The synchronous assignment fires on the call stack of `ProStatusManager()` — before any SwiftUI view body runs.
3. `Pault/Models/ProFeature.swift:64-66` — `ProFeature.isUnlocked(_:)` delegates to `ProStatusManager.shared.isProUnlocked`, so the synchronous `true` set in init() is visible to all gate checks at first render.
4. `Pault/ContentView.swift:347-356` — `if ProFeature.isUnlocked(.analytics)` wraps the Analytics toolbar Button. With `isProUnlocked = true` from step 2, the button is rendered. Line 354 carries `.accessibilityLabel("Analytics")`.
5. `PaultUITests/ScreenshotTests.swift:304` — `let analyticsButton = app.buttons["Analytics"]` resolves against the label placed in step 4. String match is exact.
6. `PaultUITests/ScreenshotTests.swift:305-306` — `analyticsButton.waitForExistence(timeout: 5)` → `analyticsButton.click()` triggers `showingAnalytics = true`.
7. `Pault/ContentView.swift:91-92` — `.sheet(isPresented: $showingAnalytics) { AnalyticsView() }` presents the sheet.
8. `Pault/AnalyticsView.swift:56-64` — `NavigationStack { Group { ... }.accessibilityIdentifier("analytics-view") }`. The `.accessibilityIdentifier` is on the `Group` **inside** the `NavigationStack` body, placing it in the rendered subtree. `if ProFeature.isUnlocked(.analytics) { analyticsContent }` renders content because Pro is unlocked.
9. `PaultUITests/ScreenshotTests.swift:316-317` — `app.groups["analytics-view"].waitForExistence(timeout: 5)` resolves. `XCTAssertTrue(...)` passes.
10. `PaultUITests/ScreenshotTests.swift:322` — `captureScreenshot(name: "06-analytics-dashboard")` executes.

**End-to-end result:** Verified by xcodebuild test exit 0 (17-01-SUMMARY.md). Screenshot captured at `/tmp/06-analytics-dashboard-final.png`. Visual sign-off by user on 2026-04-29.

**Status: COMPLETE**

---

### Flow 2 — ProStatusManager screenshot-mode sync override (UX-03)

**Claim from Phase 17:** `init()` sets `isProUnlocked = true` synchronously before `Task { await refreshStatus() }` is scheduled, eliminating the first-render race.

**Source verification (`Pault/Services/ProStatusManager.swift`):**

- Line 24: `init() {`
- Lines 25-27: `transactionListener = Task { await listenForTransactions() }` (background listener, does not affect `isProUnlocked`)
- Lines 28-32: `#if DEBUG` block — `if ProcessInfo.processInfo.arguments.contains("--screenshot-mode") { isProUnlocked = true }` — **synchronous**
- Line 33: `Task { await refreshStatus() }` — async, scheduled but not yet run
- Lines 69-75: `refreshStatus()` also contains the same `#if DEBUG` guard at its top, providing redundant coverage if the async task runs during the test.

**Result:** The synchronous override is correctly positioned. `isProUnlocked = true` is set on the same thread that calls `ProStatusManager()` before any async work is scheduled. First-render race is eliminated.

**Status: COMPLETE**

---

### Flow 3 — analytics-view AX identifier inside rendered subtree (TEST-01)

**Claim from Phase 17:** `.accessibilityIdentifier("analytics-view")` was moved from the outer NavigationStack frame into the inner rendered Group.

**Source verification (`Pault/AnalyticsView.swift:55-74`):**

```
55:    var body: some View {
56:        NavigationStack {
57:            Group {
58:                if ProFeature.isUnlocked(.analytics) {
59:                    analyticsContent
60:                } else {
61:                    upgradePrompt
62:                }
63:            }
64:            .accessibilityIdentifier("analytics-view")   ← inside NavigationStack body
65:            .navigationTitle("Analytics")
...
73:        }
74:        .frame(width: 480, height: 560)                  ← outside NavigationStack, no identifier here
75:    }
```

The identifier is on `Group` at line 57-63, which is inside `NavigationStack`'s `content` closure. `app.groups["analytics-view"]` resolves in the XCUITest hierarchy. The `.frame` modifier at line 74 is outside the NavigationStack and carries no identifier.

**Status: COMPLETE**

---

### Flow 4 — DATA-01: Attachment round-trip (ImportOrchestrator)

**Claim from Phase 14:** Both `insertPrompt` and `updatePrompt` restore Attachment stubs from `record.attachmentFileNames`.

**Source verification:**

- `Pault/ImportOrchestrator.swift:300-314` (`insertPrompt`): After `context.insert(prompt)`, iterates `record.attachmentFileNames`, constructs `Attachment(filename: name, mediaType: "application/octet-stream", fileSize: 0, storageMode: "stub", sortOrder: index)`, sets `attachment.prompt = prompt`, appends to `prompt.attachments`, calls `context.insert(attachment)`.
- `Pault/ImportOrchestrator.swift:391-409` (`updatePrompt`): Clears existing attachments via `context.delete`, removes from array, then runs identical stub-construction loop from `record.attachmentFileNames`.

Both paths are wired. Three round-trip tests exist in `PaultTests/ImportOrchestratorTests.swift` (lines documented in 14-VERIFICATION.md: `applyImport_restoresAttachmentFileNames`, `applyImport_nilAttachmentFileNames_producesNoAttachments`, `applyImport_overwrite_restoresAttachmentFileNames`).

**Status: COMPLETE**

---

### Flow 5 — CODE-01: SidebarView delegation to PromptService.filterPrompts

**Claim from Phase 14:** SidebarView `.smartCollection` case delegates to `PromptService.filterPrompts`; no inline filter reimplementation remains. `collectionCounts` and context-menu export paths follow the same pattern.

**Source verification (`Pault/SidebarView.swift`):**

- Line 82-84 (`filteredPrompts`): `.smartCollection(let collection)` → `let service = PromptService(modelContext: modelContext)` → `prompts = service.filterPrompts(prompts, collection: collection)`. 2-line delegation, no inline filter logic.
- Lines 113-119 (`collectionCounts`): Iterates collections, calls `service.filterPrompts(allPrompts, collection: collection).count`. Same delegation pattern.
- Lines 225-226 and 233-234 (context-menu export paths): Both call `service.filterPrompts(allPrompts, collection: collection)` to compute the matched prompt list. Same delegation pattern.

Total of 4 call sites in SidebarView — all consistent, no reimplementation. `PromptService.filterPrompts` is defined at `Pault/PromptService.swift:191` accepting `(_ prompts: [Prompt], collection: SmartCollection)`.

**Status: COMPLETE**

---

## Specific Checks Mandated by Audit

### Check 1 — `app.buttons["Analytics"]` resolves via `.accessibilityLabel("Analytics")`

- `Pault/ContentView.swift:354`: `.accessibilityLabel("Analytics")` present on the Analytics toolbar Button.
- `PaultUITests/ScreenshotTests.swift:304`: `app.buttons["Analytics"]` queries the label string-for-string.
- The button is inside `if ProFeature.isUnlocked(.analytics)` (line 347), which evaluates `true` at test runtime because `ProStatusManager.init()` sets `isProUnlocked = true` synchronously before first render.
- **WIRED.**

### Check 2 — ProStatusManager.init() synchronous override ordering

- `init()` body order (lines 24-34): transactionListener Task → `#if DEBUG` synchronous override (lines 28-32) → async `Task { await refreshStatus() }` (line 33).
- The `#if DEBUG` block is executed synchronously on the call stack of `init()`. The async Task is merely *scheduled* at line 33, not executed. No first-render race.
- `refreshStatus()` also contains the guard (lines 70-74) for the async path.
- **WIRED.**

### Check 3 — `analytics-view` identifier inside NavigationStack rendered subtree

- `Pault/AnalyticsView.swift:56-64`: `NavigationStack { Group { ... }.accessibilityIdentifier("analytics-view") }`. The modifier is on the `Group` inside `NavigationStack`'s content closure — in the rendered subtree.
- `.frame(width: 480, height: 560)` at line 74 is a modifier on the `NavigationStack` itself, outside its content closure; it carries no identifier.
- `app.groups["analytics-view"]` queries the `Group` node correctly.
- **WIRED.**

### Check 4 — DATA-02: All three production CopyEvent call sites use two-arg form

All production call sites found via `grep -rn 'CopyEvent(' Pault/ --include="*.swift"`:

| File | Line | Form | Two-arg? |
|------|------|------|----------|
| `Pault/PromptService.swift` | 95 | `CopyEvent(promptID: prompt.id, type: .copy)` | YES |
| `Pault/AnalyticsService.swift` | 26 | `CopyEvent(promptID: promptID, type: type)` | YES |
| `Pault/ScreenshotDataSeeder.swift` | 437 | `CopyEvent(promptID: prompt.id, type: .copy)` | YES |

All three sites use the two-arg init. `CopyEvent.swift:32` retains the legacy one-arg init for backward compatibility but no production call site uses it. DATA-02 scope was `PromptService.copyToClipboard` specifically; the other two sites were already using the two-arg form prior to this milestone.

**WIRED (all 3 sites).**

### Check 5 — UX-01 + UX-02: Proxy guard ordering and sidebar refresh

**UX-01 — AIAssistPanel proxy guard ordering:**
- `Pault/AIAssistPanel.swift:38-40`: `isProxyConfigured` property delegates to `ProxyConfig.isConfigured`.
- `Pault/AIAssistPanel.swift:64-67`: `if !isProxyConfigured { noProxyStateView } else if !hasAnyAPIKey { noKeyStateView } else { ... }`. Proxy check fires first.
- `noProxyStateView` at lines 93-110 contains `network.slash` icon, "Proxy URL not configured" text, and Open Preferences button.
- **WIRED.**

**UX-02 — Sidebar refresh button:**
- `Pault/SidebarView.swift:28`: `@State private var refreshingCollectionIDs: Set<UUID> = []`.
- `Pault/SidebarView.swift:39-63`: `refreshAICuratedCollection(_:)` inserts ID into set, calls `AIService.shared.clusterPrompts(...)`, updates `collection.promptIDs`, removes ID from set on completion or error.
- `Pault/SidebarView.swift:196`: `if collection.ruleType == .aiCurated` gate.
- `Pault/SidebarView.swift:205-215`: Button with `arrow.clockwise` or `ProgressView` spinner; `.disabled(refreshingCollectionIDs.contains(collection.id) || !ProxyConfig.isConfigured)`.
- Proxy-disabled guard matches the pattern in AIAssistPanel and SmartCollectionEditorView.
- **WIRED.**

**SmartCollectionEditorView Generate with AI button:**
- `Pault/SmartCollectionEditorView.swift:96`: `.disabled(isGenerating || allPrompts.isEmpty || !ProxyConfig.isConfigured)`.
- `Pault/SmartCollectionEditorView.swift:97`: `.help(ProxyConfig.isConfigured ? "" : "Configure proxy URL in Preferences first")`.
- **WIRED.**

### Check 6 — DOC-01/DOC-02/DOC-03: Documentation traceability and legal dates

**DOC-01:** `grep -c '| Complete |' .planning/milestones/v1.0-REQUIREMENTS.md` → 33. `grep -c '| Pending |'` → 0. All 33 v1.0 requirements show Complete; no Pending rows remain.

**DOC-02:** `requirements-completed` field present in all four Phase 04 SUMMARYs:
- `04-00-SUMMARY.md`: `requirements-completed: []`
- `04-01-SUMMARY.md`: `requirements-completed: [R2.5, R5.1]`
- `04-02-SUMMARY.md`: `requirements-completed: [R2.1, R2.2, R2.3, R2.4]`
- `04-03-SUMMARY.md`: `requirements-completed: [R5.2, R5.3]`

Verified at source via grep on each file.

**DOC-03:**
- `docs/legal/terms-of-service.md:3`: `Effective date: 2026-04-27`
- `docs/legal/privacy-policy.md:3`: `Last updated: 2026-04-27`
- No `[Launch Date]` string in either file.

**WIRED (all three).**

---

## Wiring Summary

| From | To | Via | Status |
|------|----|-----|--------|
| `PaultUITests/ScreenshotTests.swift:64` launchArguments | `ProStatusManager.init():28-32` | `--screenshot-mode` argument | WIRED |
| `ProStatusManager.init():30` `isProUnlocked = true` | `ProFeature.isUnlocked(:):65` | `ProStatusManager.shared.isProUnlocked` | WIRED |
| `ProFeature.isUnlocked(.analytics)` (ContentView:347) | Analytics toolbar Button render | `if` gate | WIRED |
| `ContentView.swift:354` `.accessibilityLabel("Analytics")` | `ScreenshotTests.swift:304` `app.buttons["Analytics"]` | AX label string match | WIRED |
| `ContentView.swift:349` `showingAnalytics = true` | `ContentView.swift:91-92` `.sheet { AnalyticsView() }` | `@State` binding | WIRED |
| `AnalyticsView.swift:64` `.accessibilityIdentifier("analytics-view")` | `ScreenshotTests.swift:316` `app.groups["analytics-view"]` | AX identifier string match, inside rendered Group | WIRED |
| `ImportOrchestrator.swift:301-313` (insertPrompt) | `Attachment` model | `record.attachmentFileNames` loop | WIRED |
| `ImportOrchestrator.swift:396-408` (updatePrompt) | `Attachment` model | `record.attachmentFileNames` loop | WIRED |
| `SidebarView.swift:83-84` `.smartCollection` case | `PromptService.filterPrompts` | delegation call | WIRED |
| `SidebarView.swift:117` `collectionCounts` | `PromptService.filterPrompts` | delegation call | WIRED |
| `AIAssistPanel.swift:64-65` proxy guard | `ProxyConfig.isConfigured` | `isProxyConfigured` property | WIRED |
| `SidebarView.swift:215` refresh button disabled | `ProxyConfig.isConfigured` | `!ProxyConfig.isConfigured` | WIRED |

---

## Orphaned Exports

None found. All exports from Phases 13–17 that are consumed cross-phase are verified wired at the call site.

---

## Missing Connections

None found.

---

## Broken Flows

None found.

---

## Unprotected Routes

N/A — no HTTP routes; native macOS app.

---

## Requirements Integration Map

| Requirement | Integration Path | Status | Issue |
|-------------|-----------------|--------|-------|
| DOC-01 | Phase 13 → `.planning/milestones/v1.0-REQUIREMENTS.md` (33 Complete rows) | WIRED | — |
| DOC-02 | Phase 13 → `04-{00..03}-SUMMARY.md` frontmatter `requirements-completed` fields | WIRED | — |
| DOC-03 | Phase 13 → `docs/legal/terms-of-service.md:3`, `docs/legal/privacy-policy.md:3` | WIRED | — |
| DATA-01 | Phase 14 → `ImportOrchestrator.insertPrompt:300-314` + `updatePrompt:391-409` → `Attachment` model | WIRED | — |
| DATA-02 | Phase 14 → `PromptService.swift:95` `CopyEvent(promptID:type:.copy)` | WIRED | — |
| CODE-01 | Phase 14 → `SidebarView.filteredPrompts:82-84` → `PromptService.filterPrompts`; collectionCounts:117; context-menu:225,234 | WIRED | — |
| UX-01 | Phase 15 → `AIAssistPanel.isProxyConfigured:38-40` → `ProxyConfig.isConfigured`; body guard chain:64-67 | WIRED | — |
| UX-02 | Phase 15 → `SidebarView.refreshAICuratedCollection:39-63` → `AIService.shared.clusterPrompts`; refresh button:205-215 | WIRED | — |
| UX-03 | Phase 17 closes Phase 15 gap → `ProStatusManager.init():28-32` (sync, before async Task) + `refreshStatus():70-74` (async path) | WIRED | — |
| TEST-01 | Phase 17 closes Phase 16 gap → `ContentView.swift:354` `.accessibilityLabel("Analytics")` + `AnalyticsView.swift:64` identifier inside rendered Group → `ScreenshotTests.swift:304,316` | WIRED | — |
| TEST-02 | Phase 16 → 16-02-SUMMARY.md documents 7/7 Phase 02 items signed off by user | SELF-CONTAINED | Human sign-off, no code wiring — documented in SUMMARY |
| TEST-03 | Phase 16 → 16-02-SUMMARY.md documents 3/3 Phase 08 items signed off by user | SELF-CONTAINED | Human sign-off, no code wiring — documented in SUMMARY |

**Requirements with no cross-phase code wiring (self-contained):**

- **TEST-02** — Pure human verification sign-off. The artifact is the 16-02-SUMMARY.md record; no code connection between phases was required or expected.
- **TEST-03** — Same as TEST-02.
- **DOC-01, DOC-02, DOC-03** — Documentation-file changes only; no cross-phase Swift code wiring required or expected for these requirements. The documentation artifacts are consumed by human readers and tooling, not by Swift call chains.

---

## Phase 17 Gap Closure Verification

Phase 17 was a gap-closure phase responding to the 2026-04-29 milestone audit. The three gaps it closed:

| Gap | Fix | Verification |
|-----|-----|-------------|
| Analytics toolbar button unreachable by `app.buttons["Analytics"]` | Added `.accessibilityLabel("Analytics")` at `ContentView.swift:354` | AX label matches test query string exactly; test passed end-to-end |
| First-render race: `isProUnlocked` still `false` on first SwiftUI frame | Added synchronous `--screenshot-mode` override in `ProStatusManager.init():28-32` before `Task { await refreshStatus() }` | Ordering verified at source; `isProUnlocked = true` set synchronously on init call stack |
| `analytics-view` AX identifier outside NavigationStack rendered subtree | Moved `.accessibilityIdentifier("analytics-view")` to `AnalyticsView.swift:64` inside the rendered `Group` within NavigationStack | Identifier at line 64 is inside `NavigationStack { Group { ... } }` body, not on `.frame` modifier outside; `app.groups["analytics-view"]` resolves; test passed |

All three gaps are confirmed closed at source level. No residual gaps found.

---

## Operational Constraint (non-blocking, informational)

**Stage Manager / XCUITest incompatibility:** macOS Stage Manager must be disabled before running screenshot tests locally or in CI. Windows under Stage Manager are not registered as top-level XCUI targets. This is a macOS 25.4 environmental constraint with no code-side fix. Documented in 17-01-SUMMARY.md and `.planning/memory/stage_manager_xcuitest.md`.

---

*Checked: 2026-04-29*
*Checker: Claude (integration-checker)*
