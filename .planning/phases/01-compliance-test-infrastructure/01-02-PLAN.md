---
phase: 01-compliance-test-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - PaultTests/TestHelpers.swift
  - PaultTests/PromptStudioModelTests.swift
  - PaultTests/BlockSuggestionEngineTests.swift
  - PaultTests/SlashCommandStateTests.swift
  - PaultTests/IntegrationTests.swift
  - PaultTests/AnalyticsServiceTests.swift
  - PaultTests/AttachmentManagerFileTests.swift
  - PaultTests/AttachmentManagerTests.swift
  - PaultTests/AttachmentTests.swift
  - PaultTests/BlockCompositionSnapshotTests.swift
  - PaultTests/BlockPlaceholderStatusTests.swift
  - PaultTests/BlockTemplateTests.swift
  - PaultTests/ConsolidatedBlockCategoryTests.swift
  - PaultTests/DiffEngineTests.swift
  - PaultTests/PaultTests.swift
  - PaultTests/PromptRunTests.swift
  - PaultTests/PromptServiceTests.swift
  - PaultTests/PromptTagTests.swift
  - PaultTests/PromptTemplateTests.swift
  - PaultTests/PromptVersionSnapshotTests.swift
  - PaultTests/SmartCollectionTests.swift
  - PaultTests/TagTests.swift
  - PaultTests/TemplateEngineTests.swift
  - PaultTests/TemplateSeedServiceTests.swift
  - PaultTests/TemplateVariableTests.swift
  - PaultTests/Models/CopyEventTests.swift
  - PaultTests/Models/PromptVersionTests.swift
autonomous: true
requirements:
  - R1.2

must_haves:
  truths:
    - "Shared TestHelpers factory exists and all SwiftData test files use it instead of per-file container setup"
    - "All existing tests pass (no regressions from refactoring)"
    - "BlockSuggestionEngine has tests for all 7+ heuristic paths including untested ones"
    - "SlashCommandState has tests covering remaining gaps in filtering and edge cases"
    - "PromptStudioModel has tests for major state transitions not currently covered"
    - "Integration test proves block composition to compiled preview pipeline end-to-end"
    - "Pure logic tests do not import SwiftData unnecessarily"
  artifacts:
    - path: "PaultTests/TestHelpers.swift"
      provides: "Shared ModelContainer factory with all 7 model types"
      exports: ["makeTestModelContainer", "makeTestModelContext"]
      min_lines: 15
    - path: "PaultTests/BlockSuggestionEngineTests.swift"
      provides: "Comprehensive heuristic path coverage"
      min_lines: 100
    - path: "PaultTests/SlashCommandStateTests.swift"
      provides: "Gap-filled filtering and selection tests"
      min_lines: 180
    - path: "PaultTests/PromptStudioModelTests.swift"
      provides: "Expanded state transition coverage"
      min_lines: 600
    - path: "PaultTests/IntegrationTests.swift"
      provides: "Block composition -> compiled preview pipeline test"
      contains: "blockComposition_compilesToPreview"
  key_links:
    - from: "PaultTests/TestHelpers.swift"
      to: "All SwiftData test files"
      via: "TestHelpers.makeTestModelContainer() / makeTestModelContext()"
      pattern: "TestHelpers\\.make"
    - from: "PaultTests/IntegrationTests.swift"
      to: "PromptStudioModel"
      via: "addToCanvas + setBlockInput + compileNow pipeline"
      pattern: "compileNow|compiledTemplate"
---

<objective>
Establish shared test infrastructure, migrate all test files to use it, fix any broken tests, and expand block editor test coverage to cover all critical heuristic paths and state transitions.

Purpose: Create a reliable test foundation that all subsequent phases build on. Every future phase inherits clean, consistent test infrastructure.
Output: TestHelpers.swift shared factory, migrated test files, expanded BlockSuggestionEngine/SlashCommandState/PromptStudioModel tests, compose-to-preview integration test.
</objective>

<execution_context>
@/Users/dev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-compliance-test-infrastructure/01-CONTEXT.md
@.planning/phases/01-compliance-test-infrastructure/01-RESEARCH.md

<interfaces>
<!-- Key types the test factory must include (all 7 SwiftData models) -->
From Pault/*.swift:
```swift
// All model types that must be listed in the shared container:
Prompt.self, TemplateVariable.self, Pault.Tag.self,
Attachment.self, CopyEvent.self, PromptRun.self, PromptVersion.self
```

<!-- BlockSuggestionEngine API to test -->
From Pault/BlockEditor/Services/BlockSuggestionEngine.swift:
```swift
struct BlockSuggestion {
    // suggestion details
}
enum BlockSuggestionEngine {
    static func suggest(canvasCategories: [ConsolidatedBlockCategory]) -> BlockSuggestion?
    static func shouldShowTokenWarning(tokenCount: Int) -> BlockSuggestion?
}
```

<!-- Existing test patterns -->
From PaultTests/PromptStudioModelTests.swift:
```swift
// Uses @MainActor struct with @Test macro
// Uses makeContext() helper (to be replaced with TestHelpers.makeTestModelContext())
```

<!-- Integration test pattern from research -->
```swift
// PromptStudioModel pipeline: addToCanvas -> setBlockInput -> compileNow
// Verify: compiledTemplate, rawTemplate, prompt.content, prompt.blockComposition, blockSyncState
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared test factory and migrate all test files</name>
  <files>PaultTests/TestHelpers.swift, PaultTests/*.swift, PaultTests/Models/*.swift</files>
  <action>
**Step 0: Run full test suite to establish baseline.**
Run `xcodebuild test -project Pault.xcodeproj -scheme Pault -only-testing PaultTests -destination 'platform=macOS'` and note which tests pass/fail. Fix any broken tests before proceeding. This is critical -- do not build new infrastructure on a broken foundation.

**Step 1: Create `PaultTests/TestHelpers.swift`:**
```swift
import SwiftData
@testable import Pault

enum TestHelpers {
    @MainActor
    static func makeTestModelContainer() throws -> ModelContainer {
        try ModelContainer(
            for: Prompt.self, TemplateVariable.self, Pault.Tag.self,
                 Attachment.self, CopyEvent.self, PromptRun.self, PromptVersion.self,
            configurations: ModelConfiguration(isStoredInMemoryOnly: true)
        )
    }

    @MainActor
    static func makeTestModelContext() throws -> ModelContext {
        ModelContext(try makeTestModelContainer())
    }
}
```

**Step 2: Migrate all test files with duplicated container setup.**
Search all test files for `ModelContainer(`, `ModelConfiguration(`, or local `makeContext()` helpers. For each file:
- Replace local container creation with `TestHelpers.makeTestModelContainer()` or `TestHelpers.makeTestModelContext()`
- Remove the per-file `makeContext()` helper function
- Keep the `@testable import Pault` and `import SwiftData` if the file still uses SwiftData types directly
- Verify the file compiles after migration

Expected files to migrate (~15 files based on research): IntegrationTests, PromptStudioModelTests, PromptServiceTests, PromptTagTests, PromptTemplateTests, AttachmentTests, AttachmentManagerTests, AttachmentManagerFileTests, AnalyticsServiceTests, TemplateSeedServiceTests, TemplateVariableTests, SmartCollectionTests, PromptRunTests, BlockCompositionSnapshotTests, PromptVersionSnapshotTests, Models/CopyEventTests, Models/PromptVersionTests, PaultTests.

**Step 3: Audit pure logic tests and strip unnecessary SwiftData imports.**
These test files should NOT need `import SwiftData` or `@MainActor` if they test pure logic:
- TagTests (tests Tag struct logic only)
- DiffEngineTests (tests diff algorithm)
- BlockSuggestionEngineTests (tests suggestion heuristics)
- ConsolidatedBlockCategoryTests (tests category enum logic)
- BlockPlaceholderStatusTests (tests status logic)
- SlashCommandStateTests (tests command state filtering)

For each: check if they actually use SwiftData types. If not, remove the import.

**Step 4: Run full test suite again to confirm no regressions.**
  </action>
  <verify>
    <automated>xcodebuild test -project Pault.xcodeproj -scheme Pault -only-testing PaultTests -destination 'platform=macOS' 2>&1 | tail -20</automated>
  </verify>
  <done>
  - TestHelpers.swift exists with makeTestModelContainer() and makeTestModelContext()
  - All test files use shared factory (no per-file container setup remains)
  - Pure logic tests have no unnecessary SwiftData imports
  - All tests that passed before migration still pass (zero regressions)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand BlockSuggestionEngine and SlashCommandState test coverage</name>
  <files>PaultTests/BlockSuggestionEngineTests.swift, PaultTests/SlashCommandStateTests.swift</files>
  <behavior>
  BlockSuggestionEngine (currently 4 tests, target ~15+ tests):
  - Test: task without role -> suggests adding role block
  - Test: 3+ categories without constraints -> suggests constraints
  - Test: default fallback (missing examples) -> suggests examples
  - Test: only context on canvas (no role, no task) -> appropriate suggestion
  - Test: all 7 categories present -> returns nil (complete canvas)
  - Test: duplicate categories on canvas -> handles gracefully
  - Test: shouldShowTokenWarning with count below threshold -> returns nil
  - Test: shouldShowTokenWarning with count above threshold -> returns warning suggestion
  - Test: shouldShowTokenWarning at exact threshold boundary

  SlashCommandState (currently 13 tests, target ~20+ tests):
  - Test: filterBlocks with no matching results -> returns empty array
  - Test: filterBlocks with special characters in query -> no crash, handles gracefully
  - Test: recordUsage deduplication (same block used twice)
  - Test: show() resets selectedIndex when called while already visible
  - Test: moveSelection with maxIndex of 0 -> stays at 0
  - Test: moveSelection with maxIndex of 1 -> wraps correctly
  </behavior>
  <action>
Read the current source files for BlockSuggestionEngine and SlashCommandState to understand all code paths. Then write tests covering every untested path identified in the research.

**BlockSuggestionEngine** -- read `Pault/BlockEditor/Services/BlockSuggestionEngine.swift` to understand the exact conditions for each heuristic path. Write tests for all 7+ code paths documented in research, plus shouldShowTokenWarning edge cases. These are pure logic tests -- no SwiftData needed.

**SlashCommandState** -- read the source file to understand filterBlocks, show/hide, moveSelection, recordUsage implementations. Write tests for all 6 gaps identified above. These should also be pure logic tests if possible.

Use Swift Testing (`@Test` macro, struct-based suites). Consider `@Suite` grouping for related test cases (Claude's discretion). Use descriptive test names that explain the scenario and expected outcome.
  </action>
  <verify>
    <automated>xcodebuild test -project Pault.xcodeproj -scheme Pault -only-testing PaultTests/BlockSuggestionEngineTests -only-testing PaultTests/SlashCommandStateTests -destination 'platform=macOS' 2>&1 | tail -20</automated>
  </verify>
  <done>
  - BlockSuggestionEngine has tests for all heuristic paths including: task-without-role, 3+-categories-no-constraints, default-examples-fallback, context-only canvas, all-categories-complete, duplicate categories, and all shouldShowTokenWarning boundaries
  - SlashCommandState has tests for: empty filter results, special character queries, recordUsage deduplication, show-while-visible reset, moveSelection with maxIndex 0 and 1
  - All new and existing tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expand PromptStudioModel tests and add compose-to-preview integration test</name>
  <files>PaultTests/PromptStudioModelTests.swift, PaultTests/IntegrationTests.swift</files>
  <behavior>
  PromptStudioModel (currently 34 tests, target ~45+ tests):
  - Test: modifier compilation effect -- adding a modifier changes compiled output
  - Test: multiple modifier stacking -- two modifiers on same block
  - Test: recompile after moveOnCanvas -- output reflects new block order
  - Test: save/restore round-trip -- blocks survive save to prompt and reload
  - Test: empty canvas compilation -- compiledTemplate is empty string
  - Test: block with no placeholders -- static snippet adds no inputs
  - Test: setBlockInput for non-existent block -- handles gracefully (no crash)

  Integration test (new):
  - Test: blockComposition_compilesToPreview -- full pipeline: create prompt, add role+task blocks, fill placeholders, compile, verify compiledTemplate contains resolved values, rawTemplate contains variables, prompt.content is updated, blockComposition has correct block count, blockSyncState is synced
  </behavior>
  <action>
Read `Pault/BlockEditor/PromptStudioModel.swift` (or wherever it lives) to understand the exact method signatures for: addToCanvas, setBlockInput, compileNow, moveOnCanvas, addModifier (or equivalent), and block composition save/restore.

**PromptStudioModel expansion:** Add tests for the 7 untested state transitions listed above. These tests need SwiftData (use TestHelpers.makeTestModelContext()). Use `@MainActor` as established by the existing test pattern.

**Integration test:** Add `blockComposition_compilesToPreview` test to IntegrationTests.swift following the pattern from the research document. This test proves the full pipeline: create prompt -> add blocks to canvas -> fill placeholders -> compile -> verify output in compiledTemplate, rawTemplate, prompt.content, blockComposition, and blockSyncState.

Adapt the test pattern from research to match actual API signatures found in the source code. The research pattern is a guide, not exact code -- method names, parameter names, and property names must match the real codebase.
  </action>
  <verify>
    <automated>xcodebuild test -project Pault.xcodeproj -scheme Pault -only-testing PaultTests/PromptStudioModelTests -only-testing PaultTests/IntegrationTests -destination 'platform=macOS' 2>&1 | tail -20</automated>
  </verify>
  <done>
  - PromptStudioModel tests cover modifier effects, modifier stacking, recompile-after-move, save/restore round-trip, empty canvas, static blocks, and non-existent block input edge case
  - IntegrationTests includes blockComposition_compilesToPreview proving the full compose-compile-preview pipeline
  - All new and existing tests pass
  - Total test count increased meaningfully from baseline
  </done>
</task>

</tasks>

<verification>
- Full test suite passes: `xcodebuild test -project Pault.xcodeproj -scheme Pault -only-testing PaultTests -destination 'platform=macOS'`
- No test file contains a local `makeContext()` or `ModelContainer(for:` outside TestHelpers.swift (except the 3 XCTest files: KeychainServiceTests, AIServiceTests, ProStatusManagerTests which are out of scope)
- BlockSuggestionEngine test count >= 12 (up from 4)
- SlashCommandState test count >= 18 (up from 13)
- PromptStudioModel test count >= 40 (up from 34)
- IntegrationTests contains blockComposition_compilesToPreview
</verification>

<success_criteria>
- Shared test container factory is the single source of truth for all SwiftData test setup
- Block editor test coverage addresses all critical heuristic paths and state transitions identified in research
- Integration test proves the core block composition pipeline end-to-end
- All tests pass with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/01-compliance-test-infrastructure/01-02-SUMMARY.md`
</output>
