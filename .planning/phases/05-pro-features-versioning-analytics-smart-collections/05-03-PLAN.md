---
phase: 05-pro-features-versioning-analytics-smart-collections
plan: 03
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - Pault/SmartCollection.swift
  - Pault/SmartCollectionEditorView.swift
  - Pault/SidebarView.swift
  - Pault/PromptService.swift
  - Pault/Prompt.swift
  - Pault/AIAssistPanel.swift
autonomous: true
requirements: [R4.3]

must_haves:
  truths:
    - "SmartCollectionFilter supports qualityScoreMin/Max, model, lastUsedWithin, and contentContains filter fields"
    - "All filter criteria AND together (no OR/NOT logic)"
    - "Smart collection editor form has pickers/toggles for all new filter fields"
    - "Sidebar smart collection rows show count badges with number of matching prompts"
    - "3 preset collections auto-created on first Pro unlock: Most Used, Recently Created, Stale Prompts"
    - "Presets use emoji icons: fire, sparkles, zzz"
    - "Presets are deletable/editable by user"
    - "AI-curated collections show lastRefreshed timestamp and Refresh button"
    - "Saved filter collections evaluate in real-time (always current)"
  artifacts:
    - path: "Pault/SmartCollection.swift"
      provides: "Extended SmartCollectionFilter with new fields, isPreset flag"
      contains: "qualityScoreMin"
    - path: "Pault/SmartCollectionEditorView.swift"
      provides: "Form fields for all new filter criteria"
      contains: "qualityScoreMin"
    - path: "Pault/SidebarView.swift"
      provides: "Count badges on collection rows, preset seeding call"
      contains: "collectionCounts"
    - path: "Pault/PromptService.swift"
      provides: "Extended filterPrompts with new filter field evaluation"
      contains: "qualityScoreMin"
    - path: "Pault/Prompt.swift"
      provides: "qualityScore: Int? persisted field"
      contains: "qualityScore"
  key_links:
    - from: "Pault/SmartCollectionEditorView.swift"
      to: "Pault/SmartCollection.swift"
      via: "Editor reads/writes SmartCollectionFilter fields"
      pattern: "SmartCollectionFilter"
    - from: "Pault/SidebarView.swift"
      to: "Pault/PromptService.swift"
      via: "Computes collection counts via filterPrompts for each collection"
      pattern: "filterPrompts.*collection"
    - from: "Pault/PromptService.swift"
      to: "Pault/AnalyticsService.swift"
      via: "Calls promptIDsRunWith(model:) for model filter"
      pattern: "promptIDsRunWith"
---

<objective>
Extend smart collections with new filter fields (quality score, model, last used, content search), add count badges to sidebar rows, create 3 preset collections on first Pro unlock, and persist quality scores from AI Assist to the Prompt model.

Purpose: Fulfills R4.3 (smart collections) -- dynamic, filter-driven prompt organization for Pro users.
Output: Extended SmartCollectionFilter, enhanced editor, sidebar count badges, preset collections, persisted quality scores.
</objective>

<execution_context>
@/Users/dev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pro-features-versioning-analytics-smart-collections/05-CONTEXT.md
@.planning/phases/05-pro-features-versioning-analytics-smart-collections/05-RESEARCH.md
@.planning/phases/05-pro-features-versioning-analytics-smart-collections/05-02-SUMMARY.md

@Pault/SmartCollection.swift
@Pault/SmartCollectionEditorView.swift
@Pault/SidebarView.swift
@Pault/PromptService.swift
@Pault/Prompt.swift
@Pault/AIAssistPanel.swift
@Pault/AnalyticsService.swift

<interfaces>
<!-- Key types and contracts the executor needs. -->

From Pault/SmartCollection.swift:
```swift
struct SmartCollectionFilter: Codable {
    var tagIDs: [UUID]
    var onlyFavorites: Bool
    var recentDays: Int?
}

@Model final class SmartCollection {
    var id: UUID
    var name: String
    var icon: String
    var sortOrder: Int
    var ruleType: CollectionRuleType  // .savedFilter or .aiCurated
    var filterJSON: String
    var promptIDs: [UUID]
    var createdAt: Date
    var lastRefreshed: Date?
    var filter: SmartCollectionFilter? { /* decoded from filterJSON */ }
}
```

From Pault/PromptService.swift:
```swift
func filterPrompts(_ prompts: [Prompt], collection: SmartCollection) -> [Prompt]
```

From Pault/AnalyticsService.swift (added in Plan 05-02):
```swift
func promptIDsRunWith(model: String) -> Set<UUID>
```

From Pault/Prompt.swift:
```swift
@Model final class Prompt {
    var lastUsedAt: Date?
    // qualityScore does NOT exist yet -- must be added
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend SmartCollectionFilter, add isPreset field, persist qualityScore on Prompt</name>
  <files>Pault/SmartCollection.swift, Pault/Prompt.swift, Pault/AIAssistPanel.swift</files>
  <action>
**SmartCollection.swift:**
1. Extend `SmartCollectionFilter` Codable struct with new optional fields (existing JSON decodes without error since JSONDecoder ignores missing keys with nil defaults):
   - `var qualityScoreMin: Int?` -- minimum quality score filter
   - `var qualityScoreMax: Int?` -- maximum quality score filter
   - `var model: String?` -- filter by LLM model used in runs
   - `var lastUsedWithin: Int?` -- days since last use; nil = no filter
   - `var contentContains: String?` -- substring search in prompt content
2. Add `var isPreset: Bool = false` stored property to SmartCollection (SwiftData-safe default for existing rows).
3. Add a static `seedPresetCollections(in context: ModelContext)` method on SmartCollection:
   - Guard: `ProFeature.isUnlocked(.smartCollections)` must be true
   - Guard: fetch count where `isPreset == true` must be 0 (prevent re-seeding)
   - Create 3 presets with `isPreset = true`:
     a. "Most Used" with icon "flame.fill" (or "flame") -- use `SmartCollectionFilter()` with no specific filter (top 10 by usage is computed at display time via AnalyticsService.topPromptIDsByUsage)
     b. "Recently Created" with icon "sparkles" -- use `SmartCollectionFilter(recentDays: 7)`
     c. "Stale Prompts" with icon "zzz" -- use `SmartCollectionFilter(lastUsedWithin: 30)` (prompts NOT used in 30+ days -- the filter logic will invert this)
   - Insert all three and save context.

**Prompt.swift:**
4. Add `var qualityScore: Int?` to Prompt model (SwiftData-safe nil default). This persists the AI quality score from Phase 4 so smart collections can filter on it.

**AIAssistPanel.swift:**
5. In the Score tab's success handler (where `QualityScore` result is received), persist the overall score to `prompt.qualityScore`. Find the line where `score = result` is set and add `prompt.qualityScore = result.overall` (or equivalent int field from QualityScore struct). Verify the QualityScore struct has an overall/total int field.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Pault -destination 'platform=macOS' 2>&1 | tail -5</automated>
  </verify>
  <done>SmartCollectionFilter has 5 new optional fields. SmartCollection has isPreset field. Prompt has qualityScore field. AI quality scores persist to Prompt model. Preset seeding function exists.</done>
</task>

<task type="auto">
  <name>Task 2: Wire filter evaluation, sidebar count badges, preset seeding, and editor form</name>
  <files>Pault/PromptService.swift, Pault/SidebarView.swift, Pault/SmartCollectionEditorView.swift</files>
  <action>
**PromptService.swift:**
1. Extend the `filterPrompts(_:collection:)` method's `.savedFilter` case to evaluate new filter fields:
   - `qualityScoreMin`: filter prompts where `(prompt.qualityScore ?? 0) >= minScore`
   - `qualityScoreMax`: filter prompts where `(prompt.qualityScore ?? 100) <= maxScore`
   - `model`: call `AnalyticsService(modelContext: modelContext).promptIDsRunWith(model: model)` and intersect with current result set by promptID. Import AnalyticsService if needed.
   - `lastUsedWithin`: filter prompts where `lastUsedAt` is within N days of now. For "Stale Prompts" preset (lastUsedWithin: 30), INVERT the logic: filter prompts where lastUsedAt is nil OR older than 30 days. Detect inversion by checking the collection name or adding a `invertLastUsed: Bool?` field to SmartCollectionFilter.
   - `contentContains`: filter where `prompt.content.localizedCaseInsensitiveContains(text)`.
2. For "Most Used" preset (empty filter), add special handling: when collection isPreset and name is "Most Used", use `AnalyticsService.topPromptIDsByUsage(limit: 10)` to get IDs and filter by those.

**SidebarView.swift:**
3. Add a computed `collectionCounts: [UUID: Int]` dictionary that maps each smart collection's ID to its matching prompt count by calling `PromptService.filterPrompts(allPrompts, collection:).count` for each collection.
4. Pass the count to the existing `FilterRow` (which already accepts `count: Int?`). Replace current `count: nil` with the computed count.
5. For AI-curated collections, show `lastRefreshed` timestamp as a subtle label (e.g., "Refreshed 2 hours ago") on the collection row.
6. Call `SmartCollection.seedPresetCollections(in: modelContext)` at an appropriate point -- on view appear when Pro is unlocked, guarded by the isPreset check to prevent multiple seedings. Use `.onAppear` or `.task` modifier.

**SmartCollectionEditorView.swift:**
7. Add form fields for the new SmartCollectionFilter fields:
   - Quality score range: two optional Stepper or Slider fields for min/max (0-100 range)
   - Model picker: TextField or Picker listing known models (query distinct PromptRun.model values from AnalyticsService, or a hardcoded list of common models)
   - Last used within: optional Stepper for days (e.g., "Used in last __ days")
   - Content contains: TextField for substring search
8. All fields optional -- when nil/empty, that filter is not applied (AND logic, per CONTEXT.md).
9. Bind the new fields to the SmartCollectionFilter being edited.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Pault -destination 'platform=macOS' 2>&1 | tail -5</automated>
  </verify>
  <done>Smart collection filters evaluate all new fields with AND logic. Sidebar shows count badges on collection rows. 3 presets auto-created on first Pro unlock. Editor form has fields for all filter criteria. Stale Prompts correctly shows prompts not used in 30+ days.</done>
</task>

</tasks>

<verification>
1. Build succeeds: `xcodebuild build -scheme Pault -destination 'platform=macOS'`
2. Existing tests pass: `xcodebuild test -scheme Pault -destination 'platform=macOS' -only-testing:PaultTests/SmartCollectionTests`
3. SmartCollectionFilter encodes/decodes with new fields (existing JSON without new fields decodes to nil defaults)
</verification>

<success_criteria>
- SmartCollectionFilter has qualityScoreMin/Max, model, lastUsedWithin, contentContains fields
- SmartCollection has isPreset field
- Prompt has qualityScore field persisted from AI quality scoring
- PromptService.filterPrompts evaluates all new filter fields with AND logic
- Sidebar shows count badge on every smart collection row
- 3 preset collections created on first Pro unlock (Most Used, Recently Created, Stale Prompts)
- Presets use flame/sparkles/zzz icons, are deletable/editable
- Editor form includes pickers/fields for all new filter criteria
- AI-curated collections show lastRefreshed timestamp
</success_criteria>

<output>
After completion, create `.planning/phases/05-pro-features-versioning-analytics-smart-collections/05-03-SUMMARY.md`
</output>
