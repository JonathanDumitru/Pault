---
phase: 07-app-store-readiness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Pault/ScreenshotDataSeeder.swift
  - Pault/PaultApp.swift
  - PaultUITests/ScreenshotTests.swift
  - docs/app-store/screenshot-capture.md
autonomous: false
requirements: [R7.3]

must_haves:
  truths:
    - "ScreenshotDataSeeder creates 8-10 realistic prompts with tags, versions, analytics events, and AI assist state"
    - "App launches in screenshot mode when --screenshot-mode launch argument is present"
    - "6 XCUITest screenshot tests capture at 2560x1600 resolution with realistic seed data"
    - "Screenshots cover the locked lineup: AI Assist, Block editor, API Runner, Library split view, Menu bar popover, Analytics dashboard"
    - "Screenshot capture documentation is updated with XCUITest-based workflow"
  artifacts:
    - path: "Pault/ScreenshotDataSeeder.swift"
      provides: "Seed data factory for screenshot mode with 8-10 prompts, tags, versions, copy events"
      min_lines: 80
    - path: "PaultUITests/ScreenshotTests.swift"
      provides: "6 XCUITest screenshot capture tests with XCTAttachment output"
      min_lines: 100
    - path: "docs/app-store/screenshot-capture.md"
      provides: "Updated screenshot documentation with XCUITest workflow and new 6-shot lineup"
      contains: "XCUITest"
  key_links:
    - from: "Pault/PaultApp.swift"
      to: "Pault/ScreenshotDataSeeder.swift"
      via: "--screenshot-mode launch argument triggers ScreenshotDataSeeder.seed(context:)"
      pattern: "screenshot-mode"
    - from: "PaultUITests/ScreenshotTests.swift"
      to: "Pault/PaultApp.swift"
      via: "app.launchArguments = [\"--screenshot-mode\"] triggers seed data path"
      pattern: "launchArguments.*screenshot-mode"
---

<objective>
Build the automated screenshot capture system for App Store submission: a seed data factory that populates the app with realistic content, a launch argument to activate screenshot mode, 6 XCUITest cases that navigate to each target screen and capture at 2560x1600, and updated documentation.

Purpose: Reproducible, high-quality App Store screenshots that showcase Pro features as the hero shots, captured automatically via XCUITest rather than manual screencapture.
Output: ScreenshotDataSeeder.swift, updated PaultApp.swift, ScreenshotTests.swift, updated screenshot-capture.md.
</objective>

<execution_context>
@/Users/dev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-app-store-readiness/07-CONTEXT.md
@.planning/phases/07-app-store-readiness/07-RESEARCH.md

@Pault/PaultApp.swift
@Pault/ContentView.swift
@PaultUITests/PaultUITests.swift
@docs/app-store/screenshot-capture.md

<interfaces>
<!-- PaultApp init and model container pattern -->
From Pault/PaultApp.swift:
```swift
@main
struct PaultApp: App {
    var sharedModelContainer: ModelContainer = { ... }()

    init() {
        CrashReportingService.install()
        appDelegate.modelContainer = sharedModelContainer
        let seedContext = ModelContext(sharedModelContainer)
        TemplateSeedService.seed(into: seedContext)
    }
}
```

<!-- SwiftData models used for seed data -->
From Pault/Prompt.swift (key fields):
```swift
@Model final class Prompt {
    var title: String
    var content: String
    var isFavorite: Bool
    var isArchived: Bool
    var editingModeRaw: String  // "plain" or "blocks"
    var tags: [Tag]?
    var versions: [PromptVersion]?
    var copyEvents: [CopyEvent]?
    var promptRuns: [PromptRun]?
}
```

<!-- Existing UI test boilerplate -->
From PaultUITests/PaultUITests.swift:
```swift
final class PaultUITests: XCTestCase {
    // Standard Xcode-generated boilerplate
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ScreenshotDataSeeder and --screenshot-mode wiring</name>
  <files>Pault/ScreenshotDataSeeder.swift, Pault/PaultApp.swift</files>
  <action>
    1. Create `Pault/ScreenshotDataSeeder.swift` with `enum ScreenshotDataSeeder`:
       - `static func seed(context: ModelContext)` — creates all seed data in a single method
       - Create 8-10 prompts covering diverse use cases:
         a. "Code Review Assistant" (plain text, tagged "development", "code-review", favorited) — this will be the AI Assist hero prompt
         b. "API Documentation Generator" (block editor mode with 4-5 blocks: system instruction, context block, output format, example block) — for block editor screenshot
         c. "SQL Query Optimizer" (plain text, tagged "database", "sql") — for API Runner screenshot showing a run result
         d. "Meeting Summary Template" (plain text, tagged "productivity", "meetings", with 2 template variables: {{attendees}}, {{date}})
         e. "Email Draft: Client Follow-up" (plain text, tagged "communication", favorited)
         f. "Bug Report Template" (plain text, tagged "development", "qa", with variables: {{component}}, {{severity}}, {{steps}})
         g. "Product Requirements Doc" (plain text, tagged "product", "documentation")
         h. "User Story Generator" (plain text, tagged "agile", "product")
         i. "Technical Interview Questions" (plain text, tagged "hiring", "development", archived)
         j. "Release Notes Writer" (plain text, tagged "documentation", favorited)
       - Create 6-8 tags: "development", "productivity", "documentation", "communication", "database", "product", "agile", "qa"
       - Create 3-5 PromptVersion entries for the "Code Review Assistant" prompt (to populate version history)
       - Create 15-20 CopyEvent entries spread across recent dates for the top 4-5 prompts (to populate analytics dashboard)
       - Create 2 PromptRun entries for "SQL Query Optimizer" with mock response text (to populate API Runner history)
       - If `ProcessInfo.processInfo.arguments.contains("--screenshot-mode-ai-streaming")`, set a UserDefaults flag that AIAssistViewModel (or equivalent) can read to display a hardcoded mid-stream state showing partial rewrite text for the AI Assist hero shot.
       - All seed data should look realistic — use actual prompt-like content, not "Lorem ipsum".
       - Use `try? context.save()` at the end.

    2. Update `Pault/PaultApp.swift` init():
       - After existing `TemplateSeedService.seed(into: seedContext)`, add:
         ```swift
         if ProcessInfo.processInfo.arguments.contains("--screenshot-mode") {
             ScreenshotDataSeeder.seed(context: seedContext)
         }
         ```
       - This ensures screenshot seed data is loaded on top of the template seed data.
       - Do NOT modify any other part of PaultApp.swift — the screenshot mode is additive only.
  </action>
  <verify>
    <automated>cd /Volumes/Drive/Projects/Software/macOS/Pault && xcodebuild build -scheme Pault -destination 'platform=macOS' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>ScreenshotDataSeeder.swift compiles and creates 8-10 realistic prompts with tags, versions, copy events, and prompt runs. PaultApp.swift seeds screenshot data when --screenshot-mode launch argument is present. Build succeeds with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: XCUITest screenshot suite and documentation update</name>
  <files>PaultUITests/ScreenshotTests.swift, docs/app-store/screenshot-capture.md</files>
  <action>
    1. Create `PaultUITests/ScreenshotTests.swift` (XCTestCase, NOT Swift Testing @Test — XCUITest requires XCTestCase):
       - `var app: XCUIApplication!` instance variable
       - `override func setUp()`: set `continueAfterFailure = false`, create `XCUIApplication()`, set `app.launchArguments = ["--screenshot-mode", "--screenshot-mode-ai-streaming"]`, call `app.launch()`
       - Helper method `captureScreenshot(name: String)`:
         ```swift
         let window = app.windows.firstMatch
         XCTAssertTrue(window.waitForExistence(timeout: 10))
         let screenshot = window.screenshot()
         let attachment = XCTAttachment(
             uniformTypeIdentifier: "public.png",
             name: name,
             payload: screenshot.pngRepresentation,
             userInfo: nil
         )
         attachment.lifetime = .keepAlways
         add(attachment)
         ```
       - Helper method `resizeWindowForScreenshot()` — resize main window to 1280x800 logical points (produces 2560x1600 on Retina 2x). Use: `app.windows.firstMatch.typeKey("", modifiers: [])` or frame-based approach. NOTE: XCUITest does not have a direct window resize API; document this as a known limitation. The test should set the window to the correct size using AppleScript via shell or accept the default window size. Add a comment noting the user should run on a 2x Retina display and verify dimensions with `sips` after capture.
       - 6 test methods in the locked screenshot order:

       a. `testShot01_AIAssist()`:
          - Navigate to the "Code Review Assistant" prompt (click in sidebar/list)
          - Open AI Assist panel (Cmd+Shift+I shortcut or click the AI Assist toolbar button)
          - Wait for the Improve tab to show mid-stream content (seeded by --screenshot-mode-ai-streaming)
          - `captureScreenshot(name: "01-ai-assist")`

       b. `testShot02_BlockEditor()`:
          - Navigate to "API Documentation Generator" prompt (block editor mode)
          - Wait for canvas and blocks to render
          - `captureScreenshot(name: "02-block-editor")`

       c. `testShot03_APIRunner()`:
          - Navigate to "SQL Query Optimizer" prompt
          - Open the Run tab (Cmd+Return or click Run tab)
          - Wait for response history to show
          - `captureScreenshot(name: "03-api-runner")`

       d. `testShot04_LibrarySplitView()`:
          - Ensure sidebar is visible (toggle if needed)
          - Select a prompt in the list to show the full split view (sidebar + list + detail)
          - `captureScreenshot(name: "04-library-split-view")`

       e. `testShot05_MenuBarPopover()`:
          - Click the status bar item to open the menu bar popover
          - Wait for popover to appear and populate
          - Use `XCUIScreen.main.screenshot()` instead of window screenshot — this captures the popover floating above the desktop (per RESEARCH recommendation for this specific shot)
          - `captureScreenshot(name: "05-menu-bar-popover")` but using screen capture for this one

       f. `testShot06_AnalyticsDashboard()`:
          - Navigate to Analytics (click Analytics in sidebar or tab)
          - Wait for charts to render with seeded copy events
          - `captureScreenshot(name: "06-analytics-dashboard")`

       - Each test should use `waitForExistence(timeout:)` before every navigation step and capture to avoid timing issues.
       - Add accessibility identifiers in comments where needed — if navigation is unreliable, note that the executor should add `.accessibilityIdentifier()` to key views.
       - NOTE: The exact XCUITest navigation queries (e.g., `app.buttons["AI Assist"]`, `app.outlines.cells["Code Review Assistant"]`) will depend on the actual accessibility tree. The executor should use Accessibility Inspector or `po app.debugDescription` to discover the correct element queries. Write the tests with best-guess queries and adjust based on actual UI hierarchy.

    2. Update `docs/app-store/screenshot-capture.md`:
       - Replace the manual screencapture workflow with XCUITest-based workflow
       - Document the 6-shot lineup matching locked decisions: 1. AI Assist (Improve tab mid-stream), 2. Block editor canvas, 3. API Runner, 4. Library split view, 5. Menu bar popover, 6. Analytics dashboard
       - Add run command: `xcodebuild test -project Pault.xcodeproj -scheme Pault -destination 'platform=macOS' -only-testing:PaultUITests/ScreenshotTests`
       - Add verification: `sips -g pixelWidth -g pixelHeight <screenshot.png>` — expect 2560x1600
       - Note: run on 2x Retina display, disable display sleep (`caffeinate -d`), light mode only
       - Document how to extract screenshots from test results: Open Xcode > Test Results > expand ScreenshotTests > attachments, or use `xcresulttool` CLI
       - Document naming: 01-ai-assist.png through 06-analytics-dashboard.png
       - Add captions for each shot (for ASC upload)
  </action>
  <verify>
    <automated>cd /Volumes/Drive/Projects/Software/macOS/Pault && xcodebuild build-for-testing -scheme Pault -destination 'platform=macOS' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>ScreenshotTests.swift compiles with 6 test methods covering the locked screenshot lineup. Each test uses XCTAttachment with .keepAlways lifetime for PNG output. screenshot-capture.md updated with XCUITest workflow, run command, and verification steps. Build-for-testing succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify screenshot quality and seed data</name>
  <files>PaultUITests/ScreenshotTests.swift</files>
  <action>Human runs screenshot tests, reviews captured images for quality, composition, and resolution correctness.</action>
  <what-built>Screenshot seed data system (ScreenshotDataSeeder.swift), 6 XCUITest screenshot tests wired via --screenshot-mode launch argument, and updated documentation.</what-built>
  <how-to-verify>
    1. Run screenshot tests: `xcodebuild test -project Pault.xcodeproj -scheme Pault -destination 'platform=macOS' -only-testing:PaultUITests/ScreenshotTests 2>&1 | tail -30`
    2. Open Xcode > navigate to Test Results > ScreenshotTests > verify 6 PNG attachments are present
    3. Verify seed data looks realistic: launch app with `--screenshot-mode` argument and browse the seeded prompts, tags, analytics
    4. Verify screenshots show the correct screens: AI Assist mid-stream, block editor, API Runner, library split view, menu bar popover, analytics dashboard
    5. Check resolution: extract a PNG from test results and run `sips -g pixelWidth -g pixelHeight <file.png>` — should be 2560x1600 on a 2x Retina display
    6. Review docs/app-store/screenshot-capture.md for accuracy
  </how-to-verify>
  <verify>Human visual review</verify>
  <done>All 6 screenshots reviewed and approved — correct screens, realistic data, proper resolution.</done>
  <resume-signal>Type "approved" if screenshots look good, or describe which shots need adjustment (navigation, timing, seed data, or composition issues)</resume-signal>
</task>

</tasks>

<verification>
1. `xcodebuild build-for-testing -scheme Pault -destination 'platform=macOS'` succeeds
2. ScreenshotDataSeeder.swift creates 8-10 prompts with diverse tags, versions, and analytics data
3. PaultApp.swift checks for --screenshot-mode and calls ScreenshotDataSeeder.seed
4. ScreenshotTests.swift has 6 test methods matching locked lineup order
5. Each test produces an XCTAttachment with .keepAlways lifetime
6. screenshot-capture.md documents XCUITest workflow with run command and verification
</verification>

<success_criteria>
- ScreenshotDataSeeder creates realistic seed data (8-10 prompts, 6-8 tags, version history, copy events, prompt runs)
- --screenshot-mode launch argument activates seed data in PaultApp.swift init
- 6 XCUITest screenshot tests compile and target the locked lineup: AI Assist, Block editor, API Runner, Library, Menu bar, Analytics
- Screenshots are captured as XCTAttachment PNG files with .keepAlways lifetime
- screenshot-capture.md replaced with XCUITest-based workflow documentation
- Build-for-testing passes with no compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-app-store-readiness/07-02-SUMMARY.md`
</output>
