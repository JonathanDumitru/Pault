---
phase: 03-storekit-2-paywall
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - Pault/PaywallView.swift
autonomous: true
requirements: [R6.2]

must_haves:
  truths:
    - "Paywall displays contextual header with triggering feature name and icon"
    - "Paywall shows Free vs Pro checkmark comparison grid"
    - "CTA button text is dynamic from StoreKit introductoryOffer, never hardcoded"
    - "Apple-required auto-renewal disclosure text appears in paywall"
    - "Privacy Policy and Terms of Service links are tappable"
    - "Restore Purchases button is present and functional"
    - "Segmented product Picker is removed (annual-only)"
  artifacts:
    - path: "Pault/PaywallView.swift"
      provides: "Compliant paywall with dynamic offer text, feature comparison, legal disclosures"
      contains: "introductoryOffer"
  key_links:
    - from: "Pault/PaywallView.swift"
      to: "Pault/Models/ProFeature.swift"
      via: "ProFeature.allCases drives feature comparison grid"
      pattern: "ProFeature\\.allCases"
    - from: "Pault/PaywallView.swift"
      to: "StoreKit Product"
      via: "Dynamic CTA from product.subscription?.introductoryOffer"
      pattern: "introductoryOffer"
---

<objective>
Rebuild PaywallView to meet Apple's App Store compliance requirements: dynamic CTA text from StoreKit introductory offer data, Free vs Pro feature comparison grid driven by ProFeature enum, Apple Schedule 2 required auto-renewal disclosure text, and Privacy Policy/Terms of Service links.

Purpose: Apple rejects apps missing auto-renewal disclosures in the paywall UI (Schedule 2 Section 3.8(b)). Hardcoded trial text breaks for ineligible users. The current PaywallView is a stub needing full compliance treatment.
Output: Fully compliant PaywallView.swift ready for App Store review.
</objective>

<execution_context>
@/Users/dev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-storekit-2-paywall/03-CONTEXT.md
@.planning/phases/03-storekit-2-paywall/03-RESEARCH.md
@.planning/phases/03-storekit-2-paywall/03-01-SUMMARY.md

@Pault/PaywallView.swift
@Pault/ProBadge.swift

<interfaces>
<!-- From Plan 01 (will exist when this runs) -->

From Pault/Models/ProFeature.swift:
```swift
enum ProFeature: String, CaseIterable {
    case aiAssist, versioning, analytics, apiRunner, smartCollections, unlimitedBlocks

    var displayName: String { ... }
    var description: String { ... }
    var sfSymbol: String { ... }

    static func isUnlocked(_ feature: ProFeature) -> Bool
    static let freeBlockLimit = 5
}
```

From Pault/Services/ProStatusManager.swift (refactored):
```swift
@MainActor @Observable
final class ProStatusManager {
    static let shared = ProStatusManager()
    static let proProductID = "com.pault.pro.annual"  // single, not array
    private(set) var isProUnlocked: Bool = false
    private(set) var availableProducts: [Product] = []
    func purchase(_ product: Product) async throws -> Bool
    func restorePurchases() async
    func loadProducts() async
}
```

Current PaywallView init signature (preserved for callers):
```swift
struct PaywallView: View {
    let featureName: String
    let featureDescription: String
    let featureIcon: String
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild PaywallView with compliance and dynamic content</name>
  <files>Pault/PaywallView.swift</files>
  <action>
**Full rewrite of PaywallView.swift.** Preserve the init signature `(featureName:featureDescription:featureIcon:)` so all existing call sites compile unchanged.

**Layout structure (top to bottom in a ScrollView):**

1. **Contextual header:** Feature icon (48pt, purple-blue gradient as current), "Unlock {featureName}" title with ProBadge, feature description text. Keep current design.

2. **Pro features bullet list:** Iterate `ProFeature.allCases`, for each show SF Symbol + displayName. Highlight the triggering feature (match by featureName or pass ProFeature case). Use checkmarks or filled circles for visual clarity.

3. **Free vs Pro comparison grid:** Two-column layout with "Free" and "Pro" headers. Rows:
   - Prompt CRUD: check / check
   - Templates & Tags: check / check
   - Search & Surfaces: check / check
   - Block Editor (5 blocks): check / check
   - AI Assist: x / check
   - Versioning: x / check
   - Analytics: x / check
   - API Runner: x / check
   - Smart Collections: x / check
   - Unlimited Blocks: x / check

4. **Dynamic CTA button:** Remove the segmented Picker entirely. Single product — get the annual product from `proStatus.availableProducts.first`. Compute CTA text dynamically:
   - Add `@State private var ctaText: String = "Subscribe"` and `@State private var isEligibleForTrial: Bool = false`
   - In `.task`, after loading products, check `await product.subscription?.isEligibleForIntroOffer`
   - If eligible and offer exists with `.freeTrial` payment mode: "Start {period} Free Trial" (derive period from offer)
   - If eligible and offer exists with `.payAsYouGo`: "Start for {offer.displayPrice}/{period}"
   - Otherwise: "Subscribe for {product.displayPrice}/year"
   - Use `product.displayPrice` not hardcoded "$59.99" — StoreKit handles localization
   - The CTA button calls `purchaseSelected()` which uses the single product (no picker selection needed)

5. **Purchase error text:** Keep inline error display below CTA (current behavior).

6. **Auto-renewal disclosure (REQUIRED by Apple Schedule 2):** Small-print section below CTA:
   - "Subscription automatically renews unless cancelled at least 24 hours before the end of the current period."
   - "Payment will be charged to your Apple ID account. Manage or cancel anytime in System Settings > Apple ID > Subscriptions."
   - Display price dynamically: "{product.displayPrice}/year" (not hardcoded)

7. **Legal links:** Row with "Privacy Policy" and "Terms of Service" as tappable links. Use `Link("Privacy Policy", destination: URL(string: "https://pault.app/privacy")!)` and same for terms. Style as `.font(.caption)` `.foregroundStyle(.secondary)`.

8. **Restore Purchases button:** Keep at bottom, styled as current (.footnote, .secondary, .plain).

**Remove:**
- Segmented Picker for product selection
- `selectedProductID` state var
- Any reference to monthly product

**Update `purchaseSelected()`:**
- Get product as `proStatus.availableProducts.first` (only one product)
- Remove picker-based selection logic

**Keep:** ProgressView during product load, dismiss on successful purchase, dismiss on successful restore.

**Frame:** Increase height to accommodate new content. Use `ScrollView` inside the sheet to handle overflow. Frame width ~420, min height ~520 or use flexible sizing.
  </action>
  <verify>
    <automated>cd /Users/dev/Desktop/Software/macOS/Pault && xcodebuild build -scheme Pault -destination 'platform=macOS' -quiet 2>&1 | tail -5 && echo "---" && grep -c "introductoryOffer\|isEligibleForIntroOffer" Pault/PaywallView.swift && echo "---" && grep -c "Picker" Pault/PaywallView.swift</automated>
  </verify>
  <done>PaywallView compiles. Contains dynamic CTA from introductoryOffer (grep count > 0). Picker removed (grep count = 0). Auto-renewal disclosure text present. Privacy Policy and Terms links present. Free vs Pro comparison grid present. ProFeature.allCases used for feature list.</done>
</task>

</tasks>

<verification>
- `xcodebuild build -scheme Pault -destination 'platform=macOS' -quiet` succeeds
- PaywallView contains `introductoryOffer` or `isEligibleForIntroOffer` (dynamic CTA)
- PaywallView contains "automatically renews" (Schedule 2 disclosure)
- PaywallView contains "pault.app/privacy" and "pault.app/terms" (legal links)
- PaywallView contains no `Picker` (segmented picker removed)
- PaywallView contains no "com.pault.pro.monthly" (monthly product removed)
- PaywallView uses `ProFeature.allCases` for feature list
</verification>

<success_criteria>
- Paywall shows contextual header, Pro features list, Free vs Pro comparison grid
- CTA button text derived dynamically from StoreKit introductory offer data
- Apple-required auto-renewal disclosure text displayed
- Privacy Policy and Terms of Service links tappable
- No hardcoded trial text, no monthly product references, no segmented Picker
- All existing PaywallView call sites compile unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/03-storekit-2-paywall/03-02-SUMMARY.md`
</output>
