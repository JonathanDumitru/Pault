---
phase: 08-final-quality-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - PaultTests/**/*.swift
  - PaultUITests/PaultUITests.swift
autonomous: true
requirements: [R8.1]

must_haves:
  truths:
    - "Full test suite (unit + UI) passes with zero failures in the distribution build"
    - "No regressions introduced by any prior phase"
    - "All deferred test issues from STATE.md are addressed or explicitly documented as non-blocking"
  artifacts:
    - path: "PaultTests/"
      provides: "All existing ~60 test files pass"
    - path: "PaultUITests/PaultUITests.swift"
      provides: "UI tests pass including launch performance"
  key_links:
    - from: "xcodebuild test -scheme Pault"
      to: "PaultTests + PaultUITests"
      via: "Xcode scheme test action"
      pattern: "Test Suite.*passed"
---

<objective>
Run the full test suite against the current codebase, fix any failures or regressions, and validate the distribution build compiles cleanly.

Purpose: Establish a green baseline before any performance, accessibility, or polish work. Bug fixes in this plan could invalidate benchmarks or accessibility attributes if done later.
Output: Zero-failure test suite, clean archive build.
</objective>

<execution_context>
@/Users/dev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-final-quality-polish/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and fix all failures</name>
  <files>PaultTests/**/*.swift, PaultUITests/PaultUITests.swift</files>
  <action>
    1. Run the full test suite: `xcodebuild test -scheme Pault -destination 'platform=macOS' 2>&1 | xcpretty`
    2. Collect all failures. For each failure:
       - Determine if it is a genuine regression (code changed) or a flaky/environment issue
       - Fix the root cause in production code or test code as appropriate
       - Re-run the specific failing test to confirm the fix
    3. Address the deferred issues from STATE.md:
       - "Compilation cache does not include modifiers in cache key" — verify this was fixed in Phase 02 (CompilationCache modifier fix). If tests still fail, fix.
       - "PromptStudioModel.placeholders() returns duplicates" — verify tests are adjusted per the decision. If new tests rely on unique behavior, adjust.
    4. After all fixes, run the full suite again to confirm zero failures.
    5. Build for archive to verify distribution build compiles: `xcodebuild build -scheme Pault -configuration Release -destination 'platform=macOS' 2>&1 | tail -5`
  </action>
  <verify>
    <automated>xcodebuild test -scheme Pault -destination 'platform=macOS' 2>&1 | tail -5</automated>
  </verify>
  <done>Full test suite passes with zero failures. Release build compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Validate distribution archive and notarization dry-run</name>
  <files>Pault.xcodeproj/project.pbxproj</files>
  <action>
    1. Build a release archive: Use the existing `build-release.sh` script if present, or run:
       `xcodebuild archive -scheme Pault -configuration Release -archivePath /tmp/Pault.xcarchive -destination 'generic/platform=macOS' 2>&1 | tail -10`
    2. Verify the archive completed successfully (check for `.xcarchive` output).
    3. Verify Hardened Runtime is enabled in the archive: `codesign -d --entitlements - /tmp/Pault.xcarchive/Products/Applications/Pault.app 2>&1 | head -20`
    4. Verify sandbox is enabled in the entitlements output.
    5. If archive fails, diagnose and fix the build issue (likely signing or entitlement config).
    6. Do NOT attempt actual notarization (requires Apple ID credentials) — just verify the archive is structurally sound.
  </action>
  <verify>
    <automated>test -d /tmp/Pault.xcarchive && echo "Archive exists" || echo "Archive missing"</automated>
  </verify>
  <done>Release archive builds successfully. Entitlements show sandbox and hardened runtime enabled. Archive is structurally ready for notarization.</done>
</task>

</tasks>

<verification>
- `xcodebuild test -scheme Pault -destination 'platform=macOS' 2>&1 | grep -E "(passed|failed)"` shows all passed, zero failed
- `/tmp/Pault.xcarchive` exists and contains a valid `.app` bundle
</verification>

<success_criteria>
- Zero test failures across all ~60 unit test files and UI tests
- Release archive builds successfully with sandbox + hardened runtime
- No deferred issues from STATE.md remain unaddressed
</success_criteria>

<output>
After completion, create `.planning/phases/08-final-quality-polish/08-01-SUMMARY.md`
</output>
