---
phase: 16-testing-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [TEST-02, TEST-03]

must_haves:
  truths:
    - "All 7 Phase 02 human verification items are signed off"
    - "All 3 Phase 08 human verification items are signed off"
  artifacts: []
  key_links: []
---

<objective>
Complete human verification sign-off for 10 items deferred from Phase 02 (Block Editor Polish) and Phase 08 (Final Quality and Polish).

Purpose: These items require runtime observation that cannot be automated -- drag-drop visual effects, VoiceOver announcements, animation smoothness, Instruments profiling, and visual polish. They were flagged as "human_needed" in their respective VERIFICATION.md files and deferred to Phase 16 for final sign-off.

Output: Human confirmation of all 10 verification items. No code changes expected.
</objective>

<execution_context>
@/Users/dev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.0-phases/02-block-editor-polish/02-VERIFICATION.md
@.planning/milestones/v1.0-phases/08-final-quality-polish/08-VERIFICATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Phase 02 human verification -- Block Editor runtime behavior (7 items)</name>
  <files>None (human verification only)</files>
  <action>
Present the 7 Phase 02 human verification items to the user for sign-off. These items were flagged as "human_needed" in 02-VERIFICATION.md. No code changes -- this is a pure verification checkpoint.

Items to verify (launch Pault and open a block-mode prompt with 3+ blocks on the canvas):

1. Drag-drop visual line indicator -- Drag a block to reorder. EXPECTED: 2pt accent-colored horizontal line appears between blocks at drop position.
2. Lift effect during drag -- Drag a block and observe while held. EXPECTED: Block scales to ~1.03x, elevated shadow, ~0.85 opacity.
3. Block expand/collapse animation smoothness -- Expand/collapse several blocks rapidly. EXPECTED: No layout jumps or dropped frames. Under Reduce Motion: no spring bounce.
4. Slash palette open speed -- With 10+ blocks, press Cmd+/. EXPECTED: Palette appears in under 100ms (feels instant).
5. VoiceOver full navigation pass -- Enable VoiceOver (Cmd+F5), navigate canvas with VO+arrows, open Actions rotor (VO+Cmd+\). EXPECTED: Each block reads "[category] block: [title], position N of M". Actions rotor offers Move Up, Move Down, Delete, Duplicate, Expand/Collapse.
6. Reduce Motion compliance -- Enable System Settings > Accessibility > Display > Reduce Motion. Reorder and expand/collapse blocks. EXPECTED: No spring bounce or slide. Instant or cross-fade transitions only.
7. First-input focus after palette insert -- Press Cmd+/, select a block with placeholder inputs. EXPECTED: Cursor lands in first input TextField immediately (no click or Tab needed).
  </action>
  <verify>
    <automated>echo "Human verification checkpoint -- no automated test. User confirms pass/fail for 7 items."</automated>
  </verify>
  <done>User has confirmed pass or fail status for all 7 Phase 02 verification items. Results recorded in SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 08 human verification -- Quality and polish runtime checks (3 items)</name>
  <files>None (human verification only)</files>
  <action>
Present the 3 Phase 08 human verification items to the user for sign-off. These items were previously approved in sub-plan checkpoints during v1.0 but are included here for final v1.1 sign-off confirmation.

Items to verify:

1. Instruments memory profiling -- Open Instruments with Leaks template targeting Pault (Release build). Exercise: add 20+ blocks, switch between all 3 surfaces, trigger AI Assist, run a prompt, navigate version history. Run 5 minutes. Then Allocations: grow canvas to 20+ blocks, shrink back. Then App Launch: 3 cold launches. EXPECTED: Zero leaks in final snapshot; memory returns to baseline after shrink; cold launch under 1 second.

2. VoiceOver walkthrough across all 3 surfaces -- Enable VoiceOver (Cmd+F5). Navigate: main window sidebar and prompt list, prompt detail view, block-mode prompt canvas, menu bar popover. EXPECTED: All elements announced with meaningful labels. No "button" without context. Logical tab order. State changes announced.

3. Visual polish and Reduce Motion verification -- Add/remove blocks (spring animation), copy prompt (toast), open/close AI Assist. Then enable Reduce Motion and repeat. Check empty states (empty collection, nonexistent search, Analytics with no data). Check typography/spacing across main window, block editor, inspector, preferences. EXPECTED: Smooth animations, instant with Reduce Motion, informative empty states, consistent typography/spacing.
  </action>
  <verify>
    <automated>echo "Human verification checkpoint -- no automated test. User confirms pass/fail for 3 items."</automated>
  </verify>
  <done>User has confirmed pass or fail status for all 3 Phase 08 verification items. Results recorded in SUMMARY.</done>
</task>

</tasks>

<verification>
All 10 human verification items from Phases 02 and 08 have been reviewed and signed off by the user.
</verification>

<success_criteria>
- All 7 Phase 02 items confirmed as passing or issues documented
- All 3 Phase 08 items confirmed as passing or issues documented
- If any items fail, issues are recorded for potential gap closure
</success_criteria>

<output>
After completion, create `.planning/phases/16-testing-verification/16-02-SUMMARY.md`
</output>
