# Notes and cleanup

## Behavior notes
- `globalHotkey` is stored in preferences but not yet configurable (hardcoded to ⌘⇧P).
- Export/import is planned (see `docs/PROMPT_FORMAT.md`) but not implemented in the macOS app target.
- `PaultCore` models are not integrated with the app target.
