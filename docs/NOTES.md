# Notes and cleanup

## Behavior notes
- The global hotkey is configurable from `Settings > Hotkey`.
- App export/import is implemented as a JSON bundle, but it is not a full-fidelity backup of every model.
- `PaultCore` remains separate from the shipping macOS app target.
- When changing user-visible behavior, update `USER_GUIDE.md`, `FEATURE_MATRIX.md`, and the relevant enterprise docs in the same change.
